# 작업 일지

## 2026-09-23 — SWC2026 APAC Cup Lucky Draw 독립 프로젝트 시작

- 배포 후 확인: 초기 구현 커밋 `7edb0db`, GitHub Pages 실행 `35767757993` 성공. 실제 HTTPS 앱에서 격리 Chrome으로 PWA manifest/controller/cache, 새 행사 재고 0·로그 없음, 기존 앱 저장 키 sentinel 보존, 오프라인 새로고침 및 영상 Range 206/1024바이트 응답을 확인했다. 운영 iPad의 데이터는 사용하지 않았다.

- 사용자 승인: `F:\Download\SWC2026 APAC Cup_Lucky Draw App`, 공개 GitHub 저장소 `SSuperWasabi/swc2026-apac-cup-lucky-draw`, Pages 배포, 앱/PWA 저장 식별자 분리. 기존 앱과 운영 백업은 보존한다.
- 기반 Figure Draw v32의 `ac2d609`에서 Git 이력과 코드를 독립 복제했다. 기존 remote를 복제본에서 제거하고 새 origin을 연결했다. 원본 작업 폴더는 변경하지 않았다.
- 신규 버전 `swc-apac-v1`; localStorage 접두사 `swc2026-apac-lucky-draw.`, IndexedDB `swc2026-apac-lucky-draw-media`, SW 캐시 `swc2026-apac-lucky-draw-v1`, manifest id/name/start scope 분리. 과거 진단 화면과 생성 스크립트도 원본 데이터를 참조하지 않도록 분리했다.
- 기존 리소스 277개를 경로·파일 크기로 대조했고 대표 메인 영상 SHA-256이 동일함을 확인했다. 중첩 Git·node_modules·운영 `.kuji`는 리소스 복사에서 제외했다. OAP 파생 폴더 및 테스트 도구를 로컬에 복사하고 Git 제외를 유지했다.
- 새 행사명, 재고 0, 기록 없음, 미디어 없음으로 시작한다. 기존 백업을 실제로 복원하거나 과거 운영 재고를 삭제하지 않았다. 다수 당첨 경품은 기존 엔진을 사용하며 상품·수량은 추후 설정한다.
- 고정 제라투 성공 문구/미등록 상품의 제라투 이미지 fallback을 일반 경품 표시로 변경했다. 등록된 경품 이미지는 그대로 유지한다. 추첨 규칙·영상 준비·오디오 로직은 변경하지 않았다.
- 신규 격리 검사: 원본 저장 키 보존, 새로운 DB, 빈 행사, 합성 `.kuji` v2 복원, 당첨 상품 3종+참가상 정확한 재고 소진, SW 활성화 시 타 앱 캐시 보존, 진단 화면 namespace 확인 통과.
- 기존 검사 통과: test-figure, test-figure-dom, test-idle-video-cycle, test-scroll-audio, test-audio-mix, test-figure-sw, test-scroll-encoding 및 실제 Chrome test-figure-browser.
- 실제 Chrome 영상 검사: 35.45MiB 메인 원본으로 12회 복귀 시 사전 준비 요소/소스 유지 및 재로드·seek·waiting 없음. 6,808,674바이트 미당첨 후보로 10회 재진입 시 IDB 읽기 1회와 동일 노드/소스, 결과 진입 시 load/seek/waiting 없음. 팝업·5초 복귀·이미지·복수 영상 fallback·삭제 중 읽기 취소 통과. 기존 제라투 기본 이미지를 기대하던 테스트는 새 일반 경품 표시와 실제 등록 이미지 검증으로 갱신 후 통과했다.
- README를 새 프로젝트 기준으로 작성하고 기존 README는 `README-FIGURE-DRAW-v32.md`로 보존했다. `handoff.md`에 사용자 승인, 경로, 최신 AE 후보, 보존 기능, 다음 작업과 새 행사 초기화 주의를 기록했다. 테스트 의존성 lock과 설치 스크립트도 추적한다.
- OAP 선택 v8·개봉 v3·당첨 v5 AE 원본이 파일 목록에서 새로 확인됐다. 파일명상 후보이며 아직 원본 내부 확인/추출/앱 적용은 하지 않았다. 기존 보라색 UI·아이콘·소환 연출은 임시 유지한다.
- 새 iPad 실기 확인은 아직 하지 않았다. 기반 앱의 과거 사용자 확인과 이번 Chrome 검증을 구분한다. 배포 후 PWA 오프라인 검증은 별도 스크립트 `test-project-pwa-browser.cjs`로 수행한다.

## 2026-09-18 — v32 사용자 확인 완료 및 전체 이력·게시용 원고 정리

- 사용자 피드백 원문: “좋았어. 모두 다 확인햇어.” v32 배포 후 메인/미당첨 영상 및 기존 기능 영향 점검에 대한 사용자 확인 완료로 기록한다. 확인 환경의 OS 버전·횟수·장시간 측정치는 추가로 제공되지 않아 추정하지 않는다.
- 구현/배포 이력: v31 `1c57dac`, v32 `379fb90`. v32 GitHub Pages 작업 `35301843858` 성공과 실제 HTML·SW·영상 모듈·화면 코드 일치를 이전 배포 작업에서 확인했다.
- README의 최신 상태와 커밋 표를 갱신하고 과거 시점의 미검증/미푸시 표현과 현재 사용자 확인을 구분했다. RESULT-VIDEO-IMPACT-REVIEW.md에도 사용자 확인 결과를 추가했다. 과거 일지를 삭제하거나 성공으로 소급 수정하지 않는다.
- `writing reference/figure-draw-work-reference.md`: 쿠지 기반 분석부터 v32까지의 구현, 철회한 접근, 검증, 자산/작업 폴더 정리, 운영 주의사항과 커밋 이력을 종합한다.
- `writing reference/figure-draw-confluence-post.md`: 타부서 공유용 컨플루언스 게시글 초안. 이번 작업은 Markdown 작성이며 컨플루언스에 직접 게시하지 않는다.
- 작업 정본은 `F:\Download\Figure draw app`이다. C 드라이브 원본의 삭제 성공 여부는 이번 기록에서 확인한 것으로 주장하지 않는다. 대화 기록 자체를 복원한 문서가 아니라 현재 저장소·기존 일지·대화에서 확인 가능한 작업을 정리한 문서다.
- 이번 변경은 문서만 대상으로 한다. 앱/SW는 v32 유지, 운영 설정·재고·로그 및 영상 원본 변경 없음. 이전 v28 ZIP은 재생성하지 않는다.

## 2026-09-18 — figure-v32 단일 미당첨 영상 사전 준비 및 영향 점검

- 요청: 기존 기능/품질에 미치는 영향을 점검하고 문제가 없으면 iPad 테스트용 커밋·푸시.
- 위험 지점: 결과 이미지 컨테이너 재생성, 결과 이탈 시 모든 영상 소스/URL 해제, 상품 팝업 클릭, 메인 영상과의 준비 경쟁. 상세 결과는 RESULT-VIDEO-IMPACT-REVIEW.md에 기록했다.
- 전용 결과 영상 노드를 기존 이미지 영역의 형제로 유지한다. 참가상 영상 키가 하나일 때만 메인 영상 준비 후 미리 디코딩하며, 이탈 후 다음 결과 전에 시작 위치를 준비한다. 동일 영상은 IDB/Blob URL/노드를 재사용한다. 복수 영상·당첨 영상은 기존 경로 유지.
- 기존 결과 진입 후 5초 복귀 규칙, 추첨/재고/로그, 결과 이미지/크롭, 팝업, 무음과 오디오 정책을 유지했다. 비활성 화면은 불투명 레이어와 inert로 가리고 진입 애니메이션을 보존한다.
- 키 변경/삭제 시 준비 취소 및 URL 해제. 늦게 반환되는 일반 결과/팝업 읽기도 화면 세대로 검증한다. 원본 영상이나 저장 스키마는 변경하지 않는다.
- 실제 Chrome에서 6.49MiB 로컬 후보로 10회 결과 반복: 파일 읽기 1회, 동일 영상 요소/소스, 결과 진입 중 load/seek/waiting 없음. 재고·로그, 준비된 메인 복귀, 팝업 중 5초 자동 복귀 중지/닫은 뒤 재시작, 당첨 이미지, 여러 영상 대체 경로, 삭제 도중 비동기 읽기 취소를 검증했다.
- 전용 영상 1개 추가 보유에 따른 iPad 메모리 영향과 최초 실행/초고속 복귀/백그라운드 상황은 실기 검증 항목이다. Chrome 결과만으로 무버퍼를 보장하지 않는다. 버전 figure-v32 / figure-draw-v32.

## 2026-09-17 — figure-v31 다음 메인 영상 사전 디코딩 (iPad 검증용)

- 사용자 요구: 메인 복귀 첫 순간부터 다음 영상의 시작이 나와야 한다. 이전 영상/포스터를 잠깐 보여준 뒤 교체하는 방식은 제외한다.
- 코드 확인: v29의 선읽기는 IndexedDB 데이터까지만 준비하며, v30도 복귀 시 `src`/`load()`로 재생 준비를 다시 시작했다. `pause()` 단독이 버퍼의 원인이라는 이전 판단은 확정할 근거가 없다. KUJI의 차이만으로 iPad 원인을 단정하지 않는다.
- `app/idle-video.js`: DOM에 고정한 현재/다음 슬롯 2개. 메인 재생 중 다음 영상을 선정하고 명시적 muted play와 프레임 콜백으로 디코딩한 뒤 0초 탐색을 완료해 정지한다. 대기 슬롯은 불투명 레이어로 가려지며 display:none으로 분리하거나 DOM을 이동하지 않는다.
- 복귀 시 준비된 요소의 표시 역할만 교체한다. 새 영상에는 소스 재설정·load·되감기를 하지 않고, 이전 슬롯은 정지/소스 해제/Blob URL 해제 후 다음 준비에 재사용한다. 다음 영상은 숨겨진 채 계속 루핑하지 않는다.
- v30 전경 포스터·localStorage 프레임 저장·페이드 전환 제거. 여백 블러용 320px 정지 이미지만 유지. 레이아웃 이벤트를 두 슬롯에 연결하고 중복 초기 bootIdle 호출 제거. 재고·로그·설정 저장 스키마는 변경하지 않는다.
- 화면 이탈, 백그라운드, 준비 중 복귀, 삭제된 목록의 늦은 IDB 응답, 재사용 슬롯의 이전 프레임 노출을 처리한다. 준비되지 않은 복귀는 진단에 warm:false로 남기며 성공으로 감추지 않는다. 첫 실행/메모리 회수/즉시 복귀는 여전히 실기 검증이 필요하다.
- 검증: 순환·기본 추첨·DOM·스크롤 오디오·오디오 믹스·서비스워커 검사 및 실제 Chrome 전체 흐름 통과. 새 브라우저 검사는 0.74MiB 샘플과 35.45MiB 원본을 각각 12회 복귀해 동일한 준비 요소/소스, 시작 위치, load/seek/waiting 없음, 자동 복귀와 예외 경로를 검사한다. Chrome 결과를 iPad 무버퍼 보장으로 해석하지 않는다.
- 배포 버전: figure-v31 / figure-draw-v31. iPad 판정은 README의 검증 항목을 따른다. 구현 승인 후 커밋·푸시 대상으로 준비했다.

## 2026-09-17 — figure-v30 iPad 메인 영상 버퍼 공백 보완

- 실기 피드백: v29의 로드 우선순위와 다음 영상 선읽기를 적용해도 iPad PWA에서 메인 루핑 영상의 첫 출력 전 버퍼 공백이 남았다.
- 확인: 실제 최종 메인 영상은 30~47초, 35~40MB이며 IndexedDB의 ArrayBuffer 전체 복원과 첫 프레임 디코딩이 완료돼야 화면이 나타나는 구조다.
- 조치: 앱에 기본 메인 포스터를 사전 캐시하고, 최근 정상 디코딩한 첫 프레임을 저용량 JPEG로 localStorage에 보관한다. 다음 시작과 메인 복귀 때 이를 즉시 표시한다.
- 실제 영상의 `playing` 이벤트 뒤 두 렌더 프레임을 기다려 포스터를 페이드아웃한다. 영상 디코더는 계속 1개만 유지한다.

## 2026-09-17 — figure-v29 메인 루핑 영상 초기 표시 개선

- 원인: 부팅 시 모든 상품 이미지, BGM 3개, 효과음 3개를 IndexedDB에서 순차로 읽은 뒤에야 메인 대기 영상을 요청해, 운영 데이터가 많을수록 영상 로드 시작 자체가 늦어졌다.
- 조치: 기존 데이터 이전을 마친 직후 메인 영상을 가장 먼저 읽고 재생 요청한다. 이미지·BGM·효과음은 그 뒤 병렬로 로드해 메인 영상 요청을 막지 않는다.
- 화면을 떠날 때 무작위 순환 큐의 다음 영상 1개만 IndexedDB에서 미리 읽는다. 복귀 시 준비된 데이터를 사용하고 이전 화면 프레임은 새 소스가 준비될 때까지 유지한다.
- 비디오 요소와 디코더는 계속 1개만 사용한다. 선읽기 데이터도 최대 1개로 제한해 v28의 이중 디코딩 제거와 백그라운드 정지 동작을 유지한다.
- 앱/서비스워커 캐시는 v29. 자동 검사와 실제 브라우저에서 초기 요청 순서, 복귀 전환, 순환 중복 방지와 단일 디코더 유지를 확인한다.

## 2026-09-16 — 공유용 작업 폴더 및 리소스 정리

- 작업 폴더 루트에 흩어진 이미지·영상·오디오와 기존 `resource/`, `sound/`, 프레임 시퀀스, 미리보기 산출물을 `resource/images`, `resource/videos`, `resource/audio`, `resource/fonts`로 종류별 분류했다.
- 최초 분류에서 기존 `resource/`의 생성 영상들을 후보로 판단해 `videos/-old/generated-candidates`에 두었으나, 이 파일들이 현장용 메인 루핑 영상과 꽝 당첨 상품 영상임을 확인했다. 운영 최종 자산으로 다시 분류해 모두 `resource/videos/final-assets/`로 이동했다.
- 현재 PWA의 URL과 배포 동작을 깨뜨리지 않도록 실행 정본은 `app/assets/`에 유지했다. 현재 적용 파일의 공유용 사본은 각 `final-assets`에, 원본·후보·이전 시안·미사용 파일은 각 `-old`에 보관한다. 기반 쿠지 참고 저장소는 `resource/project-reference/-old/`로 이동했다.
- 동일 구조를 다시 갱신할 수 있도록 경로 이탈 검사와 유형별 요약을 포함한 `scripts/organize-share.ps1`을 추가했다. `resource/README.md`에는 전달받는 부서가 정본과 보관본을 구분할 수 있도록 폴더 역할을 기록했다.
- 공유 ZIP에서는 Git 내부 이력과 재설치 가능한 `.tools/node_modules`, 로컬 브라우저·ffmpeg 패키지를 제외하고 앱 코드, 문서, 스크립트, 최종/과거 리소스를 포함한다. `scripts/create-share.ps1`로 동일한 ZIP을 다시 만들 수 있다. 폴더 정리 후 앱 회귀 검사를 다시 실행한다.

## 2026-09-16 — figure-v28 메인 루핑 영상 디코딩 부하 개선

- 메인 영상과 확대·블러 배경에서 동일 영상을 동시에 재생하던 이중 디코딩을 제거했다. 블러 배경은 첫 프레임을 최대 320px JPEG 정지 이미지로 만들어 재사용한다.
- 다른 화면 및 PWA 백그라운드에서는 대기 영상을 정지하고 메인 화면이 활성화될 때만 재생한다.
- 새 Blob 소스가 준비된 다음 이전 Object URL을 해제해 화면 복귀 시 로딩 공백과 메모리 급변을 줄인다. 앱/서비스워커 캐시는 v28.
- 타부서 공유에 대비해 README 최상단에 실행 주소, iPad 설치, 관리자 진입, `.kuji` 백업/이관, 기기별 로컬 데이터 주의사항과 최신 기능·검증 상태를 정리했다. `app/README.md`는 기반 쿠지의 과거 운영 가이드임을 명시한다.
- 설치형 PWA가 새 서비스워커 활성화 후에도 이전 화면을 유지하던 문제를 줄이기 위해 `updateViaCache:'none'`, 명시적 `registration.update()`, `controllerchange` 1회 자동 새로고침을 적용한다. 설정·재고·로그가 저장된 localStorage/IndexedDB는 변경하지 않는다.
- 자동 검사: 대기 영상 순환/단일 디코더 구조, 기본 추첨, DOM 전체 흐름, 서비스워커, 실제 Chrome의 IndexedDB→Blob 영상·정지 배경·화면 이탈 정지·소환서 드래그·Web Audio RMS를 확인한다. iPad 장시간 운용은 배포 후 실기기 확인 항목으로 남긴다.

## 2026-09-16 — figure-v27 정지 손잡이·순차 방향지시등

- 대기 중 보라색 드래그 손잡이는 레일 왼쪽 출발점에 고정하고, 골드 레일 화살표 10개만 자동차 리어라이트 방향지시등처럼 좌→우로 순차 점등한다.
- 실제 손가락 드래그 중에만 손잡이가 `--progress`를 따라 이동하며 화살표 누적 점등도 진행률에 맞춘다. 12px 화살표 선과 약 75% 전체 투명도는 유지한다.
- 앱/서비스워커 캐시를 v27로 갱신하고 실제 Chrome 검사에서 대기 손잡이에 애니메이션이 없고 화살표에만 애니메이션이 적용되는지 검증한다.

## 2026-09-15 — figure-v26 골드 그라데이션 드래그 화살표

- 사용자 요청: 소환서 좌→우 드래그 안내를 첨부 이미지처럼 반복되는 꺾쇠 화살표로 바꾸고 기존 골드 컬러를 유지.
- 기존 원형 `drag-arrow.svg` 핸들/트레일 대신 약 75% 불투명도의 보라색 캡슐 손잡이(그립 점·이중 골드 화살표), 골드 레일, 방향 화살표 5개를 박스 내부 하단에 배치. 참조 이미지는 에셋으로 사용하지 않고 CSS로 직접 구현한다.
- 숨겨진 화면에서도 애니메이션이 진행돼 진입 순간 손잡이가 중간에 보일 수 있던 문제를 제거했다. `#scr-open.active`에서만 애니메이션을 시작해 매 진입 시 레일 좌측 끝에서 출발한다. 레일 화살표는 5개에서 10개로 늘리고 간격을 촘촘하게 하며, 기존 시안의 약 8px에서 1.5배인 12px 선과 70ms 점등 간격을 적용한다. 실제 드래그 중에는 자동 애니메이션을 멈추고 손잡이 및 누적 점등이 `--progress`를 따른다. 취소 시 초기화, 개봉 확정 시 기존처럼 안내를 숨긴다. 감속 모션에서는 시작 위치에 정적 표시.
- 자동 오픈 버튼 위 안내 문구를 화면별 기존 크기의 150%로 확대하고 굵기·그림자를 보강. DOM/실제 Chrome 검사에서 손잡이 위치, 진행률별 점등 수, 투명도와 글자 크기를 검증한다. 영상·오디오·추첨 로직은 유지. 앱/캐시 v26, 커밋·푸시 전.

## 2026-09-10 — figure-v16 자동 오픈 1.8배속

- 사용자 요청: 행사 운영 시간과 대기 혼잡을 줄이기 위해 자동 오픈만 1.8배로 가속. 위험을 검토한 뒤 적용.
- 영상 playbackRate=1.8, 공유 WAV의 AudioBufferSourceNode playbackRate도 동일하게 적용. playing 시 영상 위치와 배속을 전달하고 waiting/pause 중단 처리는 유지.
- 개봉 효과음은 배속으로 음높이도 상승한다. 내장 오디오 대체 경로는 preservesPitch 및 WebKit 속성을 지원하면 해제한다. BGM·당첨 효과음·소환 연출은 가속하지 않는다.
- 다음 드래그 진입 시 개봉 영상 playbackRate를 1로 초기화한다. 원본 MP4, v14 첫 프레임 처리, v15 공유 오디오 경로, 추첨·재고 로직은 유지한다.
- 개봉 구간은 기존의 1/1.8(약 55.6%) 시간. 흰색 전환 마지막 0.3초도 실시간 약 0.167초가 되며 결과 화면의 0.4초 페이드는 유지한다. 전체 회전율이 1.8배가 되는 것은 아니다.
- 오디오 단위 검사에 1.8배 큐 및 대기/소환 1배 유지 검증, DOM/실제 Chrome 검사에 자동 오픈 배속 검증 추가. iPad 실기기에서는 동기화·끊김·BGM·후속 드래그 확인 필요.
- 앱/캐시 v16. 아직 커밋·푸시 전.


## 2026-09-10 — 전체 이력 정리 및 최신 확인 상태

아래 기존 날짜별 기록은 당시 판단을 보존한다. 과거의 “iPad 미검증”은 해당 시점의 상태다. 최신 결과는 이 절과 README를 따른다.

### 사용자 보고와 조사 흐름

1. v9 작업 중 대화 화면 오류와 내역 소실이 보고됐다. 현재 기록으로 대화 장애의 원인이나 앱 코드와의 인과관계는 확정하지 않는다.
2. 소환서 영상·드래그·자동 오픈이 막히고 로딩 대기 안내가 표시됐다. v10/v11에서 준비·입력·Blob 처리 문제를 수정했으나 Chrome 검증만으로 iPad 문제는 해결되지 않았다.
3. 기기는 iPad Pro 12.9형 3세대 / iPadOS 17.6.1, 홈 화면 앱 표시는 figure-v11이었다. Safari 탭에서도 실패해 홈 화면 앱에만 국한된 현상은 아니었다.
4. 사용자가 이전 버전 비교를 제안해 비교 도구 커밋 7e0c4f2를 추가했다. v8/v9/v11 모두 실패한 진단 결과를 받아 “이전 버전은 현재 기기에서 정상”이라는 가정을 배제했다.
5. 소환서 두 영상의 readyState=0, networkState=3, duration=null과 다른 영상의 정상 메타데이터를 비교했다. 실제 H.264 헤더와 기존 생성 명령 재현으로 인코딩 결함을 확인하여 v12에서 수정했다. 과거 정상 시점의 파일/캐시 조합은 확인하지 못했다.
6. 연속 BGM 감소 및 당첨 효과음은 v13에서 수정했고 사용자가 해결됐다고 확인했다. 이후 남은 자동 오픈 직전 마지막 컷 노출을 v14에서 수정했으며 사용자도 애니메이션 수정 성공을 확인했다.
7. BGM이 간헐적으로 정상/무음이라는 추가 보고가 있었고, 사용자가 **자동 오픈은 무음, 드래그는 감소 정상**이라는 조건을 확인했다. 이 관찰과 코드 경로 차이를 근거로 v15를 수정했다.
8. 현재 v15 iPad 결과는 아직 받지 않았다. 이전 해결 보고를 v15 실기기 검증으로 대신하지 않는다.

### 진단 자료와 비교 도구

- 사용자 파일: F:/NAVER WORKS/figure-v8-diagnostics.json, figure-v9-diagnostics.json, figure-v11-diagnostics.json. 진단 데이터로 분석했으며 운영 설정으로 적용하지 않았다.
- app/review/의 v8/v9/v11 화면은 운영 설정·재고와 격리한다. 생성은 scripts/prepare-version-review.cjs, 진단은 app/media-diagnostics.html을 사용한다.
- 입력이 막힌 증상과 영상 메타데이터/디코딩 실패를 구분했다. 영상, 코드, 캐시, 실제 기기 출력은 별개 검증 대상이다.

### v15 구현 및 검증 상세

- app/scroll-audio.js: 기존 forward 디코딩 버퍼를 clips.open에 연결하여 추가 다운로드 없이 자동 오픈에 재사용한다.
- app/figure.js: 자동 오픈 활성 상태와 내장 오디오 대체 여부를 관리한다. 버퍼가 있으면 video는 muted로 재생하며 playing에서 currentTime 위치로 WAV를 시작한다. waiting/pause에서 소리를 멈추고 재개 시 위치를 다시 맞춘다. 종료·오류·대기 루프·화면 이탈에서 활성 상태를 정리한다.
- app/index.html / app/sw.js: figure-v15 / figure-draw-v15로 갱신했다. v12 호환 영상과 v14 첫 프레임 처리는 유지했다.
- test-scroll-audio: open 버퍼 재사용과 오프셋 검사. test-figure-dom: 영상 무음, playing 위치, waiting 중단, BGM 감소 상태 검사.
- test-figure-browser: 단순 볼륨 함수 호출 대신 실제 revealScroll() 중 오디오를 측정한다. RMS 정상 0.137708 → 감소 0.034665(약 25.2%) → 복원 0.138426, 효과음 0.282204. 영상 무음 및 공유 오디오 경로도 확인했다. Chrome 측정값이며 iPad 출력 측정값은 아니다.
- v15에서 test-figure / test-figure-dom / test-scroll-audio / test-audio-mix / test-figure-browser 및 git diff --check 통과.
- 버퍼가 없을 때 내장 영상 오디오 대체 경로는 남아 있다. 이 경로에서 iPad BGM이 유지되는지와 OS 내부 오디오 세션 상태는 미확인이다.

### 커밋·게시 상태

| 단계 | 커밋 | 변경 |
| --- | --- | --- |
| 기반 | 8666e80 | 쿠지 v31 복제 |
| v7 | ee4edbf | 소환서/소환 연출, 오디오, UI |
| v8 | 7ee9060 | BGM 한 곡 연속 루핑 |
| v9 | ec217dc | iOS 영상 배경 배치 수정 |
| v10 | c15eb48 | 준비 상태 및 입력 복구 |
| v11 | b49b159 | 지연 로딩, 첫 프레임, Blob 적용 시점 |
| 비교 진단 | 7e0c4f2 | v8/v9/v11 격리 비교 및 진단 |
| v12 | bb90e11 | iPad 호환 H.264 인코딩 |
| v13 | 2e04d5b | BGM GainNode 및 효과음 resume |
| v14 | 97727f3 | 자동 오픈 전 마지막 프레임 숨김 |
| v15 | 3afd03d | 자동 오픈 공유 Web Audio |

사용자의 “푸쉬하자” 승인 후 git push origin main 성공: **97727f3..3afd03d main -> main**. 대상은 SSuperWasabi/event-game-figure-draw 저장소다. 문서 갱신 시작 시 main과 origin/main은 일치했다. Pages 배포 완료나 iPad v15 테스트 완료까지 확인한 것은 아니다.

이전 게시 과정에서 자동 승인 검토 차단이 반복됐고 사용자가 7e0c4f2와 대상 저장소/main을 명시해 승인했다. 게시 승인과 기능 검증은 별개이며 푸시 성공만으로 버그 해결을 선언하지 않는다.

### 이번 문서 변경과 남은 작업

- README의 초기 미배포 문구, 자동 오픈 내장 오디오 설명, CSS 펼침 설명을 현재 구현에 맞게 교정했다. 버전별 결과, 테스트 명령, 진단 도구 및 iPad 확인 항목을 추가했다.
- 기존 작업 기록은 보존했다. 이번 수정은 README/WORKLOG뿐이며 앱 버전을 올리지 않는다. 로컬 미추적 이전 시안 MP4/JPG 4개는 포함하지 않는다.
- 다음 확인은 iPad v15에서 드래그/자동 오픈 반복, BGM 25% 감소/복귀, 당첨 효과음, 시작 프레임, 백그라운드 복귀다. 행사 전 백업/복원·오프라인·운영 재고 검증도 남아 있다.
- 이후 수정은 재현 경로와 진단 근거를 기준으로 범위를 정하며 Chrome 검사와 iPad 사용자 확인을 구분해 기록한다.

## 2026-09-10 — figure-v15 자동 오픈 오디오 경로 통일

- 사용자 실기 확인: 드래그는 BGM 감소 정상, 자동 오픈만 BGM 무음. 코드 차이는 자동 오픈이 native video audio를 켜고 드래그는 공유 Web Audio를 쓰는 것이었다. iPad 오디오 세션의 직접 로그는 없으므로 OS 내부 원인까지 확정하지 않는다.
- 자동 오픈은 이미 디코딩한 forward WAV를 clips.open으로 재사용한다. 영상은 무음, playing에서 위치를 맞춘 WAV 재생, waiting/pause에서 중지, 종료/이탈에서 소리를 정리한다. 로딩 실패 시에만 기존 native audio를 사용한다.
- v14 첫 프레임/탐색 처리는 유지한다. DOM 검사로 무음 영상·WAV 오프셋·버퍼링 중단을 검증하고 실제 Chrome 검사에서 자동 오픈 중 BGM 신호를 측정한다. iPad 최종 확인은 별도다.

## 2026-09-10 — figure-v14 자동 오픈 시작 프레임

- 이전 재생의 마지막 프레임이 남은 상태에서 video-ready를 먼저 붙이고 currentTime=0 탐색을 요청해, 비동기 탐색 전 마지막 컷이 보일 수 있었다.
- 표시 클래스를 먼저 제거하고 탐색 요청 후 준비 여부를 판단한다. seeking 중인 영상은 준비 완료로 취급하지 않으며, seeked 후 표시/자동 오픈을 이어간다. 준비 이벤트에서도 탐색 요청 후 상태를 다시 판정한다.
- readyState=4가 유지되는 지연 탐색을 재현한 회귀 검사로 이전 마지막 프레임 비노출 및 재고 보존을 확인한다. v13 오디오 처리와 v12 영상 파일은 변경하지 않았다.

## 2026-09-10 — figure-v13 iPad BGM 감소 및 효과음 복구

- 기존 감소는 HTMLMediaElement.volume 저장/복원에 의존했다. iOS에서 이 속성의 제어가 제한되며 startBgm 재호출은 0.4로 덮어써 감소 상태를 잃을 수 있었다. BGM을 GainNode에 연결하고 감소 상태를 별도로 관리한다.
- 소환서 전용 컨텍스트와 효과음 컨텍스트를 공유하도록 변경했다. 사용자 touchend/pointerup/click/keydown에서 활성화하며 효과음은 running 외 상태에서 resume 완료 후 실행한다. 실제 iPad 효과음 실패의 정확한 상태 로그는 아직 없으므로 interrupted 처리는 확인된 코드 누락의 보완이다.
- volume 쓰기를 무시하는 모의 환경에서 gain=0.1 유지, 반복 시작/음소거 해제/복원, interrupted 효과음 재생을 검사했다. Chrome 실제 오디오 RMS는 정상 0.13868 → 감소 0.03455(약 24.9%) → 복원 0.13831, 업로드 효과음 0.28220이었다. 기존 DOM/소환서 오디오/추첨 검사 통과. iPad 출력은 미검증.
- 참가자 영상과 v12 호환 인코딩은 그대로 유지한다. 앱/서비스 워커 버전은 v13이다.

## 2026-09-10 — figure-v12 소환서 인코딩 호환성 결함

- 사용자가 제공한 v8/v9/v11 진단에서 scroll-idle-video와 scroll-video는 모두 readyState=0, networkState=3, duration=null로 실패했다. 메인/소환 연출은 Blob에서도 readyState=4였다. 단순 자동재생 차단이나 Blob 전체 실패로 설명할 수 없다.
- 배포 소환서 두 파일의 AVC/SPS level_idc=62, num_units_in_tick=1, time_scale=30720을 확인했다. 정상 소환 연출은 level_idc=41, time_scale=60이었다. 해당 iPad 공식 사양은 H.264 High Level 4.2까지다: https://support.apple.com/en-us/111979
- 기존 변환 명령을 원본에 다시 실행해 Level 6.2와 `MB rate (31104000) > level limit (16711680)` 경고를 재현했다. 기존 생성 명령은 프레임 레이트/인코더 시간 기준을 고정하지 않았다.
- 두 MP4를 원본에서 fps=30, enc_time_base=1:30, High Level 3.1, yuv420p로 재생성했다. 구간 경계 101/30, 개봉 GOP=1, 원본과 별도 WAV는 유지했다. 재발 방지용 실제 비트스트림/전체 디코딩 검사를 추가했다.
- 앱/캐시 v12. 사전 캐시 요청 cache=reload로 오래된 HTTP 캐시의 영상 재사용을 막는다. 참가자 동작 로직은 이번 수정에서 바꾸지 않았다.
- 인코딩 검사, 실제 Chrome 지연 재생·자동 오픈·드래그, 기존 추첨·SW 검사를 통과했다. iPad에서 수정본 성공은 아직 검증하지 않았다. 과거 정상 시점에 어떤 파일/캐시가 사용됐는지는 현재 자료로 확정할 수 없다.

## 2026-09-10 — figure-v11 실제 영상 지연 로딩 회귀 수정

- 배포 MP4 두 파일의 HTTP 200, video/mp4, 로컬 SHA-256 일치를 확인했다. 배포 v10은 Chrome 정상 로딩에서는 재생됐다.
- v10 준비용 play 직후 pause하는 경로를 제거하고, 첫 프레임 전에는 대기 영상을 숨기지 않는다. 독립 포스터 이미지를 영상 아래에 두어 영상 요소의 포스터가 사라져도 빈 박스가 되지 않도록 했다.
- 실제 영상 응답을 보류하는 Chrome 검사에서 자동 오픈 정지를 재현했다. 준비 이벤트에서 0초 탐색이 readyState를 되돌리는 문제를 수정하고 seeked도 처리한다.
- 실제 후속 드래그 검사에서 Range 미지원 서버 원본 URL 탐색 문제를 확인했다. 진행 중 완료된 Blob을 버리지 않고 다음 화면 진입에 적용한다.
- 실제 MP4 지연 로딩/자동 오픈/후속 드래그 취소 검사를 scripts/test-figure-browser.cjs에 추가했다. 미디어 이벤트는 메모리에 최근 80개만 기록하며 외부로 전송하지 않는다. iPad 자체 재현/해결 확인은 아직 없다.

## 2026-09-10 — figure-v10 소환서 준비 및 입력 복구

- Blob 다운로드 성공 여부와 첫 프레임 준비를 입력 시작의 필수 조건으로 삼던 검사를 제거하고, 사용자 제스처에서 영상 준비 재생을 시도한다.
- 다운로드에 10초 제한을 적용하고 실패하면 원본 URL을 유지한다. 드래그·추첨 중에는 다운로드 결과로 영상 주소를 교체하지 않는다.
- 준비 전 드래그 위치를 유지하고 메타데이터 이벤트에서 반영한다. 자동 오픈은 준비 후 이어지며 화면 이탈·취소 시 대기를 해제한다. 준비 실패 12초 후 재시도할 수 있고 추첨은 확정하지 않는다.
- 대기 영상 재생 실패를 기록하고 무음 재생을 재시도한다. 앱/서비스 워커 버전은 v10으로 갱신한다.
- DOM 회귀 검사에 준비 전 드래그 및 대기 중 화면 이탈 시 재고 보존을 추가했다. iPad 실기기 재생은 미검증이다.

현재 동작 기준은 `README.md`의 「현재 상태」 절이다. 이 문서는 무엇을 왜 바꿨는지의 기록이다.

## 2026-09-09 — Claude 인수, 소환서 흐름 안정화, 제라투 소환 연출

### 인수 시점

- GPT 세션(크레딧 소진으로 중단)이 남긴 상태를 그대로 이어받았다. 커밋은 초기 1개(`8666e80`, kuji v31 복제)뿐이었고, 소환서 선택 → 드래그 개봉 → 결과 흐름 전체(`app/figure.js`, `app/figure.css`, `app/draw-engine.js`, `app/scroll-audio.js`, `app/sw.js`, `app/index.html` 변경, `scripts/`, `app/assets/figure/`)가 미커밋이었다.
- 인수 직후 검사 4종(`scripts/test-figure.cjs`, `test-scroll-audio.cjs`, `test-figure-sw.cjs`, `test-figure-dom.cjs`) 통과를 확인했다.

### 1. 소환서 대기 루프(원본 03:10까지) 무음

- 원인: `sacred-idle.mp4`에는 오디오가 있으나 `#scroll-idle-video`에 `muted` 속성이 있고 코드가 해제하지 않았다. 이전 세션이 "무음 반복"으로 설계한 것.
- 조치: 대기 구간 오디오를 `sacred-idle.wav`로 뽑아 Web Audio(드래그 사운드와 같은 경로)로 영상 위치에 맞춰 반복 재생한다. Web Audio를 쓸 수 없으면 영상 트랙을 켠다. 관리자 음소거를 따른다. BGM 25% 덕킹은 드래그 중에서 소환서 화면 전체로 넓혔다(볼륨 튐 방지).

### 2. 드래그 완료 후 개봉 영상이 03:11부터 다시 재생

- 원인 후보: 기존 코드는 드래그 완료 시 마지막 프레임 위치에서 `play()`를 호출해 33ms 재생 후 `ended`로 결과 화면에 갔다. 끝 근처 탐색 위치를 `duration`으로 맞추는 브라우저에서는 `play()`가 처음부터 다시 재생한다. Chrome(마우스·터치·헤디드·헤드리스)에서는 재현되지 않았다.
- 조치: 드래그 완료(98% 이상에서 놓음)는 영상을 재생하지 않고 곧바로 흰색 → 결과 화면으로 전환한다(`completeScrollDrag` → `finishScrollReveal`). 자동 오픈은 기존대로 전체 재생 후 `ended`.

### 3. "여전히 무음" 보고의 실제 원인

- 사용자가 VS Code 내장 미리보기(웹뷰 안의 샌드박스 iframe)로 테스트하고 있었다. 그 환경은 오디오·자동재생 권한이 위임되지 않아 영상은 나와도 소리가 억제되고 `AudioContext`도 풀리지 않는다. Chrome 탭에서는 정상.
- 사운드·자동재생 확인은 Chrome 탭 또는 실제 iPad에서 한다. 1·4번의 Web Audio 경로 변경은 필수는 아니지만 효과음 경로 통일과 Range 미지원 서버 안정성 때문에 유지했다.

### 4. 제라투(피규어) 당첨 시 전체 화면 소환 연출 (신규)

- 시퀀스: 드래그 완료/자동 오픈 종료 → 흰색 → 전체 화면 오버레이 `#summon-stage`(원본 비율 클립 중앙 + 같은 영상 확대·블러 배경) 위로 흰색이 0.4초에 걷힘 → 클립 재생 → 종료 시 0.4초 페이드 → 기존 결과 화면(번개·색종이·팡파르). 참가상은 흰색에서 바로 결과 화면.
- 사운드: `zeratu-summon.wav`를 Web Audio로 `playing` 시점부터 영상 위치에 맞춰 재생, 버퍼링(`waiting`) 시 함께 정지. Web Audio 불가 시 영상 트랙 사용.
- 안전장치: 재생 실패·`ended` 누락(영상 길이 + 3초) 시 결과 화면으로 이동. 연출 중 처음으로/유휴 복귀 무시. iPad에서 제스처 밖 재생(자동 오픈 경로)에도 소리가 나도록 소환서 화면 진입 시 영상 요소를 한 번 `load()`.
- 로컬 서버(Range 미지원)에서 블러 배경이 되감기고 메인 클립이 버퍼링하는 문제 → 개봉 영상과 같은 방식으로 파일을 한 번 Blob으로 받아 두 요소가 공유(`prepareBlobVideo`).
- 결과 카드 안의 상품 영상은 이전대로 무음 반복. 소리는 그 앞의 소환 연출이 담당.

### 5. 화면 수정

- 메인: 이벤트명 텍스트("제라투 피규어 드로우") 제거, 로고 락업만.
- 소환서 선택: 부제 "당신의 손끝에서 시작되는 소환" 제거.
- 소환서 열기: 제목 "소환서를 펼쳐주세요 · N번 소환서"를 같은 글꼴·크기로 한 줄에. 영상을 선택 카드와 같은 보라 그라데이션·보라 테두리·글로우 박스(정사각형)에 배치. 박스 아래 `drag-arrow.svg`(신규 제작) 핸들이 트랙을 따라 왼쪽→오른쪽으로 반복 이동하는 드래그 안내 애니메이션 + 문구 "스크롤을 오른쪽으로 밀거나 자동 오픈 버튼을 클릭하세요". 드래그·개봉 중 숨김, 감속 모션 설정 시 정지.
- 결과: 다른 피규어 화면과 같은 아레나 배경.
- 실수 복구: 정규식이 기존 쿠지 상품 선택 화면(S2)의 부제를 먼저 지운 것을 발견해 원복했다.

### 6. 에셋 파이프라인

- `zeratu summon.mp4`(1918×1080 가로, HEVC 10-bit) → 사용자가 `zeratu summon_34.mp4`(1080×1440 세로, HEVC 10-bit, 30fps)로 교체 요청.
- 원본 위·아래 112행이 완전한 검은 띠(ffmpeg cropdetect, 가장자리 YAVG 측정)라 `crop=1080:1216:0:112` 후 H.264 8-bit(CRF 22, AAC 48kHz)로 변환. 포스터 `zeratu-summon.jpg`, Web Audio용 `zeratu-summon.wav` 생성. 스크립트 `scripts/prepare-zeratu-summon.py`.
- `scripts/prepare-sacred-scroll.py`에 `sacred-idle.wav` 생성 추가.
- 함께 온 `zeratu summon_34/zeratu summon_34.MP3`는 영상 내장 오디오와 같은 트랙(7.1초, 동일 음량)이라 사용하지 않았다.
- 원본 미디어(루트 mp4/mp3/jpg, `resource/`, `sound/`, `zeratu summon_34/`, `main looping_4/`)는 `.gitignore`로 제외. `app/assets/figure/`의 미참조 파일(`scroll-looping-2.mp4`, `scroll3-*`)은 커밋하지 않았다(이전 시안 산출물, 로컬에만 남음).

### 7. 검증 방법

- 검사 4종에 추가: 대기 베드 오프셋/반복/음소거/폴백, 소환 클립 큐/클램프, 드래그 완료 직행, 소환 연출 재생·차단·종료·참가상 건너뛰기, 제목·드래그 안내·부제 제거. 기존 여정 테스트는 재고 비례 추첨의 무작위성으로 간헐 실패할 수 있어 참가상 전용 재고로 고정.
- 실제 브라우저: `.tools/node_modules/playwright-core` + 시스템 Chrome(헤디드/헤드리스, autoplay 플래그 유무, 마우스·CDP 터치)로 드래그·자동 오픈·소환·대기 사운드 흐름을 계측하고 1024×1366(iPad)·1536×1876 뷰포트 스크린샷으로 화면을 확인했다.
- 환경 메모: 이 PC의 Git Bash에서 `python`은 스토어 스텁이라 정적 서버는 Node로 띄우거나 사용자가 띄운 Python 3.14 `http.server`(포트 8000, 127.0.0.1)를 쓴다. 에셋 변환은 `.tools/imageio_ffmpeg/binaries`의 ffmpeg를 직접 호출. `index.html`/`sw.js`/`figure.js`에 CRLF가 섞여 있어 LF로 정리했다.
- 버전: `APP_VER` figure-v7 / 서비스 워커 캐시 figure-draw-v7.

### 8. BGM 한 곡 연속 루핑 옵션 (커밋 `ee4edbf` 이후)

- 설정 > 배경음악 카드에 **재생 방식**(화면별 전환 / 한 곡 연속 루핑)과 **연속 재생 곡**(대기·선택·뽑기 슬롯) 선택을 추가했다(`cfg.bgmMode`, `cfg.bgmSingleSlot`).
- 구현: `startBgm`을 figure.js에서 감싸 연속 모드면 어떤 슬롯 요청이든 고른 슬롯으로 바꾸고, 이미 그 슬롯이 재생 중이면 아무것도 하지 않는다(재시작·볼륨 초기화 없음). 그래서 `scrollMix`의 25% 덕킹·복귀와 음소거가 그대로 동작한다.
- 검사: DOM 테스트에 연속 모드에서 슬롯 유지·덕킹 볼륨 유지·화면별 모드 전환·관리자 컨트롤 저장을 추가. 버전 figure-v8 / figure-draw-v8.

### 9. iPad 메인 화면 영상 배치 깨짐 대응, 아레나 배경 제거 (2026-09-10)

- 보고: iPad에서 대기 영상이 좌상단에 원본 크기로 그려지고, 뒤에 아레나 배경이 남아 보임(업로드 영상은 1080×1440 HEVC, 회전 태그 없음).
- 조치: 메인 화면의 아레나 배경 이미지와 기본 루프 영상(`idle.mp4`)을 제거해 업로드 영상만 쓴다. 블러 배경은 `<video>`에 `filter`를 거는 대신 사본 영상 위에 `backdrop-filter` 베일(`#idle-video-veil`, `#summon-veil`)을 얹는 방식으로 바꿨다(iOS Safari에서 filter가 걸린 video는 object-fit이 무시될 수 있음). 사본 영상은 영상 비율이 화면과 3% 이상 다를 때만 표시·재생하고(`syncIdleBackdrop`, `loadedmetadata`/`resize`), 3:4 iPad에 3:4 영상이면 디코딩 1개만 쓴다. `#idle-video`는 `top/left/right/bottom` 명시 + `object-fit:contain`. 버전 figure-v9 / figure-draw-v9.
- iPad 실기 확인은 아직 못 했다. 재현되면 iPadOS 버전과 업로드한 영상 파일을 확인한다.

### 남은 확인·후속 과제

- 실제 iPad에서 터치 드래그, Web Audio 사운드(무음 모드 스위치 영향), 자동 오픈 경로의 소환 클립 사운드, 두 1080p 영상(클립 + 블러 배경) 동시 디코딩 부하 확인. 부하가 크면 블러 배경용 저해상도 변환본을 따로 두는 방법이 있다.
- GitHub Pages 배포 후 서비스 워커 프리캐시·오프라인 동작, 기존 쿠지 앱과 같은 origin에서 캐시 정리 범위 확인.
- 행사 수량·확률·참가상 콘텐츠 입력, 백업·복원 사전 검증(`KUJI-REFERENCE-ANALYSIS.md` 후속 항목).
- 당시 검토한 영상 트랙만 사용하는 경량화는 현재 권장하지 않는다. v15는 자동 오픈도 공유 Web Audio로 통일했으므로 되돌리려면 iPad BGM·효과음 재검증이 필요하다.

 v16 검증 결과: test-figure / test-figure-dom / test-scroll-audio / test-audio-mix / test-figure-browser 및 git diff --check 통과. 실제 Chrome 자동 오픈 playbackRate=1.8, BGM RMS 0.137708 → 0.034414(약 25.0%) → 0.138314, 효과음 0.282204 확인. iPad 결과는 미확인.

## v16 현장 운영 화면 수정

- 메인 ZERATU 제거, LUCKY DRAW로 변경. 최대 글자 크기 90px → 135px(150%), 좁은 화면은 반응형 축소. 브랜드 묶음은 높이 1/3, TOUCH는 2/3 지점에 배치하고 타이틀 부유 애니메이션 제거.
- 관리자 > 설정 > 피규어 드로우에 소환서 선택 화면 숨김 추가. 기본 OFF. ON이면 무작위 소환서 번호로 개봉 화면 직행, 뒤로가기는 메인. 번호 선택만으로 추첨·재고 차감하지 않음. cfg.hideScrollSelection으로 저장하여 기존 설정 백업에 포함.
- 참가상 상단 메시지 변경, F상 배지 숨김, 노란 상품명 영역을 요청한 두 줄 비당첨 안내로 교체. 상품 이미지와 기존 재고·추첨 규칙은 유지.
- 자동 오픈 1.8배 및 공유 WAV 동기화 포함. 아직 로컬 작업이며 iPad 최종 검증과 푸시 전.

## v17 메인 화면 및 설정 가시성

참가상 상단은 “아쉽네요!”로 단축. LUCKY DRAW 아래 같은 폰트 EVENT 추가. 세로 화면의 중앙 16:9 영상 위 여백에 브랜드/타이틀, 아래 여백에 150% TOUCH 배치. 가로 화면은 상하 오버레이로 대응. 하단 좌측 @copyright Com2uS, 중앙 실제 APP_VER, 우측 Built by SSUPERWASABI 표시. 관리자 스크린샷에 존재하던 작은 숨김 체크박스를 별도 “현장 운영 · 시퀀스 간소화” 카드의 큰 스위치로 개선. 기본 OFF 및 즉시 저장 동작 유지. 앱/캐시 v17, 로컬 수정이며 푸시 전.

### v17 영상 비율별 자동 배치

실제 대기 영상 videoWidth/videoHeight와 contain 표시 크기로 배치 계산. 가로 영상은 여백이 충분하면 타이틀/TOUCH를 영상 위아래 여백에 배치하고, 세로·정사각형 영상 및 여백 부족 시 상하 오버레이 배치. 영상 변경 loadedmetadata/emptied, 영상 크기 변경, 창 크기 변경 및 메인 복귀 시 갱신. 폰트 로딩 완료 후 재계산. 영상 미등록 시 16:9 기준. 타이틀 하향 24px 조정 의도와 150% TOUCH, 하단 표기는 유지. 오디오/영상 재생 상태는 변경하지 않음.

## v18 배치 구분 및 버전 교정

이전 자동 배치는 가로·세로의 좌표 차이가 작았으며 버전을 v17로 남겨 테스트 구분이 어려웠다. 앱/캐시를 v18로 갱신. 가로는 실제 영상 위아래 여백, 세로는 타이틀 중심 화면 1/3+24px 및 TOUCH 중심 74%로 명확히 분리했다. 공간 부족 시 화면 안으로 제한한다.

## v19 세로 영상 중앙 묶음 배치

사용자 확인: 가로 영상은 2번 이미지의 기존 배치를 유지하고 세로 영상만 중앙에 모은다. 로고/타이틀 실제 높이 + 간격(화면 높이 4.5%, 28~72px) + TOUCH 높이를 합산해 전체 묶음을 수직 중앙에 배치한다. 가로 영상 위치 계산은 변경하지 않았다. 앱/캐시 v19, 커밋·푸시 전.

## 2026-09-10 — v19 작업 마감 기록: 커밋·푸시 및 iPad 업데이트 반영

이 절은 위 v19 배치 기록에 이어 작성한 최신 상태다. 이전 절의 “커밋·푸시 전”은 당시 상태이며 아래 결과로 갱신한다.

### 최종 반영 내용

- 메인 ZERATU 제거, LUCKY DRAW / EVENT 두 줄 적용. Com2uS Store 로고와 같은 중앙 축에 정렬하고 TOUCH를 기존 대비 150%로 확대했다.
- 가로 영상은 사용자가 지정한 2번 이미지의 배치를 유지한다. 실제 contain 영상 크기를 계산하여 여백이 충분하면 타이틀은 위 여백, TOUCH는 아래 여백에 배치한다. 여백이 부족한 화면은 상하 오버레이로 처리한다.
- 세로 영상은 로고·타이틀과 TOUCH를 한 묶음으로 수직 중앙에 배치한다. 실제 요소 높이와 간격(화면 높이 4.5%, 최소 28px·최대 72px)을 합산하므로 이전 v18의 서로 멀리 떨어진 고정 비율 배치를 대체한다.
- 영상 메타데이터 로딩·영상 크기 변경·영상 초기화·창 크기 변경·메인 복귀·폰트 준비 완료 시 위치를 다시 계산한다. 배치 함수는 재생이나 오디오 상태를 변경하지 않는다.
- 메인 하단 왼쪽 @copyright Com2uS, 가운데 실제 앱 버전, 오른쪽 Built by SSUPERWASABI를 표시한다.
- 참가상 상단은 “아쉽네요!”, F상 배지는 숨김, 노란 상품명 영역은 “아쉽게도 당첨을 놓쳤어요! / 다음 기회를 노려보아요!” 두 줄이다. 상품 이미지와 추첨·재고 규칙은 유지한다.
- 관리자 > 설정 최상단 “현장 운영 · 시퀀스 간소화” 카드에 큰 “소환서 선택 화면 숨김” 스위치를 배치했다. 사용자 첨부 화면에 기존 작은 체크박스가 있었으나 찾기 어려워 가시성을 개선했다. 기본 OFF, 변경 즉시 저장. ON이면 번호만 자동 선택해 개봉 화면으로 이동하고 뒤로가기는 메인으로 이동한다. 번호 선택만으로 재고를 차감하지 않는다.
- 자동 오픈 1.8배속과 공유 WAV 동기화, BGM 25% 감소·복귀, 드래그 진입 시 1배속 초기화를 유지한다. 개봉 효과음 음높이 상승은 기존 배속 구현 특성이다.

### 검증과 한계

- v16에서 기본·DOM·오디오·실제 Chrome 검사를 통과했다. 선택 화면 생략 시 재고와 로그 보존, 설정 저장, 참가상 메시지 검사를 추가했다.
- v17 자동 배치에서 Chrome에 가로/세로 메타데이터를 주입하여 위치 전환을 확인했다. 당시 좌표 차이가 작고 버전을 올리지 않아 사용자 테스트 구분이 어려웠다. v18에서 버전을 교정하고 두 배치를 더 명확히 구분했다.
- v19에서는 사용자의 최종 의도 확인 후 세로만 중앙 묶음으로 변경했다. Chrome 1024×1366에서 묶음 중심 682.992px / 화면 중심 683px / 요소 간격 61.484px를 확인했다. 이 검사는 영상 메타데이터를 모의 지정한 배치 검사이며 iPad 실제 영상 재생 검사는 아니다.
- 앞선 실제 Chrome 검사에서 1.8배 자동 오픈, BGM 약 25% 감소·복귀와 효과음 신호를 확인했다. 마지막 배치 변경 후 전체 오디오 검사를 다시 수행한 것으로 기록하지 않는다. 커밋 전 git diff --check 통과.

### 커밋·원격 반영

| 커밋 | 포함 범위 | 상태 |
| --- | --- | --- |
| ce1e68e | v16 현장 운영 흐름·참가상·메인 변경, 자동 오픈 1.8배속, 문서·회귀 검사 | main 푸시 완료 |
| 9bf42c8 | v17~v19 타이틀/영상 비율별 배치, 큰 관리자 스위치, 하단 표기, 작업 문서 | main 푸시 완료 |

사용자 “커밋 푸쉬해줘” 요청으로 v19를 커밋했고 git push origin main이 성공했다. 원격 결과는 3afd03d..9bf42c8 main -> main, 저장소는 SSuperWasabi/event-game-figure-draw다. 현재 앱 APP_VER=figure-v19, 서비스 워커 CACHE=figure-draw-v19.

PC 테스트 주소는 http://localhost:8000/app/ 이며 로컬 Node 서버로 제공했다. 배포 앱 주소는 https://ssuperwasabi.github.io/event-game-figure-draw/app/ 이다. 로컬 서버는 PC에서 실행 중일 때만 접근할 수 있다.

### iPad 사용자 확인 및 남은 사항

- 푸시 후 사용자가 홈 화면 앱에 아직 반영되지 않는다고 보고했다가 곧 “오 되었어”라고 업데이트 반영을 확인했다.
- 따라서 홈 화면 앱 반영 지연은 사용자 보고상 해소됐다. 지연 원인을 서비스 워커나 Pages로 단정할 직접 로그는 수집하지 않았다. 앱 데이터 삭제나 추가 코드 변경으로 해결한 것은 아니다.
- 이번 답변은 업데이트 반영 확인이며 모든 기능의 최종 테스트 성공 보고는 아직 아니다. iPad에서 영상별 배치, 선택 화면 숨김, 1.8배 자동 오픈, 드래그, BGM 감소·복귀 및 당첨 효과음의 최종 결과는 별도로 남겨야 한다.
- 원본 및 미참조 이전 시안 MP4/JPG 4개는 커밋하지 않았다. 이번 마감 기록은 README/WORKLOG만 갱신하며 앱 버전을 변경하지 않는다.

## 2026-09-14 — v20 소환서 내부 드래그 안내

외부 화살표를 드래그하는 현장 관찰에 따라 안내를 소환서 박스 내부 하단으로 이동. 화살표는 왼쪽 시작점에 고정하고 트레일로 방향 안내. pointer-events:none으로 박스가 터치를 받으며 드래그/개봉 중 안내를 숨긴다. 기존 드래그 및 자동 오픈 유지. 앱/캐시 v20, 커밋·푸시 전.

## 2026-09-14 — v21 화살표 드래그 진행률 연결

화살표와 트레일을 기존 --progress에 연결하여 오른쪽/왼쪽 드래그에 맞춰 이동한다. 드래그 중 안내를 숨기던 규칙을 제거하고 자동 개봉 확정 시 숨김은 유지한다. 취소·대기 루프 복귀 시 진행률을 0으로 초기화한다. 영상 탐색·추첨 규칙·오디오 경로는 유지. 앱/캐시 v21, 로컬 변경이며 커밋·푸시 전.

## v22 피규어 당첨 시간 제한

관리자 설정에 피규어 전체 쿨다운 ON/OFF 및 구간별 최대 1개 ON/OFF, 구간 분 단위 길이, 운영 시작 날짜/시각 추가. 기본 둘 다 OFF로 기존 설정 유지. 쿨다운 분은 기존 기본 설정에서 입력하고 시간 제한 저장으로 함께 저장한다. 구간은 시작 시각 기준으로 반복하며 종료 시각이나 하루 자동 재고 보충은 없다. 둘째 날은 운영 시작 날짜/시각과 당일 재고를 설정한다. 구간 미당첨 수량은 이월하지 않으며 정확한 소진 보장 없음. 두 제한 ON이면 모두 충족해야 피규어 당첨 가능. 제한 시 확률 100% 설정도 참가상으로 전환하고 참가상 없으면 차단. 추첨 확정 직전에 재검사하며 이미 확정한 결과의 영상 재시도는 재추첨하지 않는다. 기존 원자적 재고/로그 저장으로 새로고침 이후에도 로그에서 제한을 재계산한다. 로그 삭제는 제한 이력도 지우므로 운영 중 삭제하지 않아야 한다. 기기 로컬 시계/로그 기준으로 여러 기기 간 공유 제한은 지원하지 않는다. 상품별 기존 쿠지 쿨다운 대신 피규어 전체 공통 제한이다. 앱/캐시 v22, 커밋·푸시 전.

## v23 5구간 당첨 배정

구간 제한을 구간당 1개 배정 모드로 변경. 기본 길이 24분(수정 가능), 총 5구간 고정. 저장 시 구간별 무작위 시각을 생성해 설정/백업에 보존하고 동일 시작시각·길이 재저장이나 새로고침으로 다시 뽑지 않는다. 시각 이후 해당 구간 첫 참여자는 확률 설정 대신 피규어 당첨으로 처리하며 이 모드에서는 쿨다운을 무시한다. 로그에 해당 구간 피규어 당첨이 있으면 추가 당첨 차단. 시작 전·5구간 종료 후에는 참가상만 진행. 참여자/피규어 재고 없으면 지급 불가, 미지급분 이월 없음. 둘째 날 시작 날짜·시각과 재고 별도 설정. v22 복합 제한 설명은 이 정책으로 대체. 앱/캐시 v23, 로컬 변경.

## v24 구간 수 고정 제거

운영 시작 시각과 구간 길이만 설정하며 5구간 종료 조건 제거. 설정에 저장한 난수 시드와 구간 번호로 각 당첨 시각을 재현하여 새로고침·동일 설정 재저장 시 시각이 바뀌지 않음. 구간당 한 명 지급 후 다음 구간까지 제한, 참여자 없으면 미지급 및 이월 없음. 피규어 소진 후 참가상으로 진행하고 참가상도 없으면 중지. 당일 재고만 입력하며 별도 운영 종료 시각은 없음. v23의 고정 5구간 정책을 대체. 앱/캐시 v24, 미푸시.

## v25 대기 영상 중복 없는 무작위 순환

메인 진입 시 등록 영상 목록을 섞은 순서에서 하나씩 선택한다. 전체를 한 번씩 선택한 뒤 다시 섞으며 이전 묶음 마지막 영상과 다음 묶음 첫 영상의 연속 중복도 방지한다(영상 2개 이상). 현재 영상 자체의 반복 재생은 유지한다. 영상 추가/삭제 또는 앱 새로고침 시 순환을 새로 시작한다. 앱/캐시 v25, 커밋·푸시 전.
