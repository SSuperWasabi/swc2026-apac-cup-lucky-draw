# SWC2026 APAC Cup Lucky Draw — 작업 인수인계

작성: 2026-09-23. 새 프로젝트의 기준 문서. 기존 채팅이 자동 이전되는 것은 아니므로 다음 작업은 이 파일과 README, WORKLOG를 먼저 읽는다.

## 1. 사용자 승인과 범위

- 프로젝트명: SWC2026 APAC Cup_Lucky Draw App
- 새 작업 폴더: `F:\Download\SWC2026 APAC Cup_Lucky Draw App`
- GitHub: `SSuperWasabi/swc2026-apac-cup-lucky-draw`, 공개 저장소 + GitHub Pages 승인
- 기존 Figure Draw는 보존하고 독립 파생 앱으로 개발한다.
- 사용자는 `.kuji` 백업을 보유하고 있으나 새 행사 기준으로 초기화할 예정이다. 실제 백업을 자동 복원하거나 삭제하지 않는다.
- 여러 상품이 당첨 경품이 될 예정이다. 상품명·수량·개별 확률은 아직 확정되지 않았다. 임의로 실제 행사값을 설정하지 않는다.
- 기존 기능 검증 후 새로운 브랜딩 에셋을 적용한다.

## 2. 계승 기준과 완료한 설정

- 원본 앱: `F:\Download\Figure draw app`
- 원본 저장소: https://github.com/SSuperWasabi/event-game-figure-draw
- 기반: figure-v32, 커밋 `ac2d609cb584ce21ed35a8b32183f80605828791`
- 전체 Git 이력을 독립 복제하고 새 origin만 연결했다. 원본의 remote·코드·데이터는 수정하지 않았다.
- 새 버전: `swc-apac-v1`, 캐시 `swc2026-apac-lucky-draw-v1`
- localStorage, IndexedDB, SW 캐시, manifest id/name, 앱 제목과 과거 진단 화면 namespace를 분리했다.
- 기본 재고 0, 기록 없음, 업로드 미디어 없음. 기본 상품 행은 운영 경품이 아닌 등록용 빈 틀이다.
- 추첨 엔진의 `figure`는 당첨 경품 분류이며 다수 상품을 지원한다. 고정 제라투 성공 문구와 이미지 fallback은 일반 경품 표시로 변경했다.
- 보라색 레이아웃·아이콘·기존 소환 영상은 기반 디자인으로 임시 유지한다. OAP 적용 완료로 설명하지 않는다.

## 3. 반드시 보존할 동작

- 재고 비례 기본 추첨, 선택적 고정 확률, 구간 배분·쿨다운, 숨김·묶음 상품 규칙
- 재고/로그 단일 저장 및 실패 시 추첨 미확정
- 12개 소환서 선택·랜덤 선택·선택 화면 생략 설정
- 취소/역방향 드래그에서 미추첨, 자동 오픈 1.8배속, 끝 프레임 재시작 방지
- 대기 핸들은 좌측 고정, 골드 레일 화살표만 순차 점등
- Web Audio BGM 덕킹·효과음·단일 BGM 모드
- 메인 다음 영상 사전 디코딩, 준비된 요소 재사용, 전경 포스터/이전 영상 임시 표시 금지
- 단일 미당첨 영상 재사용, 복수 영상은 기존 경로, 팝업과 결과 진입 기준 5초 복귀
- `.kuji` v2 및 분할 백업 호환성. 백업 포맷의 `app:'kuji'`는 유지한다.

## 4. 리소스 기준

AE 원본: `C:\Users\jasonbae\Downloads\(0709) swc2026_oap_APAC_Final`

로컬 복사본: `resource/oap/ae-choose-your-scroll-260922/` (파생 영상·이미지·배치 자료). 원본 AE 프로젝트 전체를 자동 복제한 것은 아니다. AE는 외부 소스를 참조하므로 원본 폴더 구조를 함부로 변경하지 않는다.

2026-09-23 목록에서 확인한 후보:

- 선택: `swc2026_CHOOSE_YOUR_SCROLL_iPad_INTRO_LOOP_v8_260923.aep`
- 개봉: `swc2026_OPEN_YOUR_SCROLL_iPad_v3_260923.aep`
- 당첨: `swc2026_WIN_PRIZE_iPad_template_v5_260923.aep`

파일명상 최신 후보이며 AE에서 열어 내용 검증한 상태는 아니다. 이전 합의의 v7과 달라졌을 수 있으므로 다음 단계에서 원본과 출력물을 확인해 기준 버전을 확정한다.

기존 `resource/`는 원본 앱에서 복사했다. `.git` 중첩 폴더, node_modules, `.kuji` 운영 백업은 리소스 복사에서 제외했다. `.tools`에는 재현용 검증 의존성과 ffmpeg를 별도 복사했다. 모든 이 자료는 Git 제외 대상이며, 실제 배포용 최적화 자산만 `app/assets`로 승격한다.

## 5. 선택 화면 구현 방향 — 다음 단계

1. 최신 AE를 읽기 전용으로 확인하고 작업용 사본에서 추출한다.
2. 배경만 담긴 루프 영상, 카드/버튼 PNG 또는 WebP, 로고, 프레임, 텍스트 규격을 분리한다.
3. 기준 화면 크기·fps·좌표계·측정 시점을 고정한다. AE Position을 HTML 좌상단 좌표로 그대로 복사하지 않는다.
4. 요소별 최종 좌상단 위치·표시 크기·투명 여백·겹침 순서를 layout.json에 기록한다. 기존 상위 layout.json은 v7/v8과 일치 여부를 검증해야 한다.
5. 시작/끝 값·시간차·이징·반복·입력 허용 시점을 motion.json과 사람이 읽는 명세에 기록한다.
6. 기존 선택 함수와 실제 버튼을 연결하고 웹 등장 애니메이션을 적용한다. 화면 전체 MP4 위에 좌표 버튼만 덮는 방식을 기본안으로 삼지 않는다.
7. 화면 진입/퇴장/재진입, 미디어 준비 경쟁, 화면비와 터치 위치, iPad 성능을 확인한다.

v7 문서의 참고값은 등장 시작 0.38초, 요소별 0.45초, 간격 0.055초, 아래 40px에서 페이드 인이다. v8 실측 전 이를 최신 값이라고 단정하지 않는다.

## 6. 운영 초기화 주의

새 namespace에서 빈 행사로 시작하므로 기존 iPad 데이터를 삭제할 필요가 없다. 보유 백업을 가져오면 옛 행사명·재고·기록도 복원된다. 실제로 가져올 경우 원하는 설정/미디어만 계승하고 운영 시작 전 재고·기록·확률·일정을 새 행사에 맞게 정리해야 한다. 이는 아직 실행하지 않았다. 저장 데이터는 기기별이며 서버 동기화나 서버 기반 참가자 제한이 아니다.

## 7. 검증과 다음 세션

검증 결과는 WORKLOG 최신 항목을 따른다. 새 `scripts/test-project-isolation.cjs`는 원본 키 보존, 새 DB 이름, 새 행사 초기값, 합성 `.kuji` 호환, 다수 상품 재고, 캐시 정리 범위와 과거 비교 화면 격리를 검사한다. 실제 사용자 백업이나 운영 브라우저는 사용하지 않는다.

다음 세션 요청 예시:

> 이 폴더의 handoff.md와 README.md를 읽고 이어서 작업해줘. 기존 Figure Draw 폴더는 수정하지 마. 먼저 최신 AE 선택 화면 후보 v8을 확인하고, 개별 에셋과 배치·움직임 명세를 추출해 앱 적용안을 검토해줘.

코드 변경 시 새 앱의 APP_VER와 SW 캐시 버전을 함께 올린다. 현재 앱 주소: https://ssuperwasabi.github.io/swc2026-apac-cup-lucky-draw/app/
