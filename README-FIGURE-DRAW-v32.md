# Figure Draw

사내 피규어 배포 이벤트용 가챠 PWA 작업 프로젝트.

- GitHub: https://github.com/SSuperWasabi/event-game-figure-draw
- Pages: https://ssuperwasabi.github.io/event-game-figure-draw/
- 앱 직접 주소: https://ssuperwasabi.github.io/event-game-figure-draw/app/
- 기반: [기존 쿠지 PWA](https://github.com/SSuperWasabi/event-game-gatcha), v31, 커밋 a1c264c21a304d5e3a0b83fbb79ece4418277a4f

## 빠른 실행 및 타부서 공유

현재 앱 버전은 **figure-v32**, 서비스워커 캐시는 **figure-draw-v32**이다. 2026-09-18 사용자가 배포 후 확인을 마치고 “좋았어. 모두 다 확인햇어.”라고 보고했다. 메인·미당첨 영상 개선과 기존 기능 영향 점검의 사용자 확인을 완료로 기록한다. 이는 사용자가 확인한 환경의 결과이며 모든 기기·장시간·메모리 부족 조건의 무버퍼를 보장한다는 뜻은 아니다. 기능·품질 영향 점검은 [RESULT-VIDEO-IMPACT-REVIEW.md](RESULT-VIDEO-IMPACT-REVIEW.md)를 참고한다.

- 전체 작업 참고 자료: [writing reference/figure-draw-work-reference.md](<writing reference/figure-draw-work-reference.md>)
- 컨플루언스 게시용 원고: [writing reference/figure-draw-confluence-post.md](<writing reference/figure-draw-confluence-post.md>)
- 실제 작업 정본: `F:\Download\Figure draw app`. 이 문서 갱신에서는 앱 코드를 변경하지 않으므로 버전은 v32를 유지한다.

- 브라우저 실행: https://ssuperwasabi.github.io/event-game-figure-draw/app/
- iPad 설치: Safari에서 위 주소를 연 뒤 공유 → **홈 화면에 추가**
- 관리자 진입: 메인 화면 좌측 상단을 3초 안에 5회 터치 → PIN 입력(초기값 `0000`)
- 권장 운영: 기기 1대당 별도 설정 후 관리자 화면에서 `.kuji` 백업과 당첨 CSV를 저장

설정·경품 이미지·업로드 영상·재고·로그는 서버나 GitHub가 아니라 **각 기기의 localStorage/IndexedDB에만 저장**된다. 다른 부서나 새 기기에 URL만 전달하면 앱은 열리지만 운영 데이터는 자동 공유되지 않는다. 기존 기기의 관리자 **설정 내보내기**로 `.kuji` 파일을 만든 뒤 새 기기에서 **설정 가져오기**를 사용해야 한다. 운영 중 PWA 삭제, Safari 웹사이트 데이터 삭제, 브라우저 초기화를 하면 로컬 데이터가 사라질 수 있으므로 먼저 백업한다.

배포 후 설치형 PWA는 새 서비스워커가 활성화되면 한 번 자동 새로고침한다. 그래도 버전이 바뀌지 않으면 Safari에서 `?v=버전`을 붙인 앱 주소를 한 번 열고 20초 정도 기다린 뒤 홈 화면 PWA를 완전히 종료하고 다시 실행한다. 메인 화면 하단 중앙과 관리자 설정 하단에서 현재 `figure-vXX`를 확인할 수 있다.

## 현재 상태 — 2026-09-18

메인은 LUCKY DRAW / EVENT와 확대된 TOUCH를 사용한다. 등록한 대기 영상은 중복 없는 무작위 순환으로 선택한다. v31은 현재 영상과 다음 영상용 요소를 최대 2개 유지하며, 현재 영상이 재생되면 다음 영상의 시작 프레임까지 미리 디코딩하고 0초에 정지한다. 복귀 시 준비된 다음 영상 요소 자체를 표시·재생하며 소스 교체나 되감기를 하지 않는다. 여백용 블러는 320px 정지 이미지이고 전경 포스터는 사용하지 않는다. 이전 메인 영상은 화면 이탈 시 정지하고, 다음 영상은 준비를 위한 짧은 재생만 수행한다. PWA 백그라운드에서는 두 영상을 모두 정지한다.

참가자 흐름은 메인 → 소환서 직접/랜덤 선택 → 드래그 또는 자동 오픈 → 소환 연출/결과 → 무입력 5초 후 메인이다. 소환서 화면의 손잡이는 대기 중 왼쪽 출발점에 고정되고 골드 화살표 10개만 좌→우로 순차 점등한다. 실제 터치 드래그에서만 손잡이가 이동한다. 자동 오픈은 영상과 공유 WAV를 1.8배속으로 재생하며, 소환서 화면·개봉 연출 중 BGM은 평소의 25%로 감소했다가 복귀한다.

추첨은 재고 비례 방식과 별도 피규어 확률 방식, 전체 쿨다운, 시작 시각 이후 반복 구간당 1개 배정 방식을 지원한다. 재고와 로그는 한 번에 저장하며 저장 실패 시 추첨을 확정하지 않는다. 상세 변경 근거와 검증 범위는 `WORKLOG.md`에 기록한다.

### 최근 수정과 확인 상태

**최종 확인: 2026-09-18 사용자 확인 완료.** 아래 과거 버전의 “미검증·대기·미푸시” 표현은 해당 작성 시점의 이력이다. 기기 모델·OS·반복 횟수·장시간 시험 수치는 이번 확인 메시지에서 별도로 제공되지 않아 새로 추정하지 않는다.

- **v32 미당첨 영상**: 참가상 영상 키가 하나이면 전용 재생기/Blob URL을 보존한다. 메인 영상 준비 후 시작 프레임까지 미리 준비하고, 결과 이탈 후 다음 사용 전 되감는다. 결과 진입 시 파일 재읽기·재생기 재생성·되감기를 하지 않는다. 기존 5초 복귀·팝업·추첨·무음 규칙은 유지하며, 여러 영상 설정은 기존 경로를 사용한다. 배포 커밋 `379fb90`, 사용자 확인 완료. 영상 1개 추가 보유에 따른 메모리 사용은 향후 장시간 운영에서도 관찰한다.

- **v31 다음 영상 사전 준비**: 포스터·이전 영상 임시 표시를 제거했다. 다음 영상 요소를 메인 체류 중부터 준비해 복귀 시 그대로 사용한다. 초기 `bootIdle()` 중복 호출도 제거했다. 35.45MiB 원본으로 Chrome 12회 버튼 복귀에서 준비된 요소/소스 유지 및 재로딩·탐색·waiting 없음, 자동 복귀·목록 변경 취소·백그라운드 정지를 검사했다. 배포 커밋 `1c57dac`, v32 배포 후 사용자 최종 확인에 포함된다. 준비 전에 돌아오는 경우는 `warm:false`로 기록하며 무버퍼 성공으로 계산하지 않는다.
- **v30 이전 접근(철회)**: 첫 프레임 준비 전 포스터를 표시하고 실제 재생 후 페이드 전환했다. 사용자 경험에 맞지 않아 v31에서 제거했다.
- **v29 메인 영상 초기 표시**: 앱 시작 시 메인 영상을 이미지·BGM·효과음보다 먼저 IndexedDB에서 읽는다. 사용자가 메인을 떠난 동안 다음 순환 영상 1개만 미리 읽어 결과 후 복귀 지연을 줄이며 영상 디코더는 계속 1개만 사용한다.
- **v28 메인 영상·PWA 갱신**: 메인/블러 배경의 이중 영상 디코딩을 단일 영상+정지 프레임 배경으로 변경했다. 화면 이탈·백그라운드에서 영상을 정지하고, 새 Blob 준비 후 이전 URL을 해제한다. 새 서비스워커 활성화 시 자동 새로고침하고 `updateViaCache:'none'`으로 업데이트를 확인한다.
- **v26~v27 드래그 안내**: 보라색 캡슐 손잡이, 골드 레일, 12px 화살표 10개를 적용했다. 대기 손잡이는 왼쪽에 고정하고 화살표만 85ms 간격으로 순차 점등하며 실제 드래그에서만 진행률을 따른다.
- **v25 대기 영상 순환**: 등록 영상을 섞은 목록에서 한 번씩 사용하고 다음 묶음 경계의 연속 중복도 방지한다.
- **v24 시간 배정**: 운영 시작 시각부터 설정 길이의 구간을 계속 계산하고 구간별 무작위 시각 이후 첫 참여자에게 피규어를 배정한다. 고정 구간 수와 자동 이월은 없다.
- **v12 영상 호환성**: v8/v9/v11 iPad 진단에서 소환서 MP4 두 개만 메타데이터 로딩 전 실패했다. H.264 Level 6.2와 잘못된 시간 기준을 재현하고 30fps·High Level 3.1로 재인코딩했다. 인코딩/디코딩 검사를 통과했고 이후 사용자는 소환서 동작 복구를 보고했다. 과거 정상 시점의 파일/캐시는 확정할 수 없다.
- **v13 BGM·효과음**: BGM GainNode 기본값 0.4, 감소값 0.1로 제어하며 감소 상태를 별도로 보존한다. 효과음·소환서 AudioContext를 공유하고 사용자 입력으로 활성화하며 resume 완료 후 효과음을 재생한다. 사용자가 해결을 확인했지만 이후 자동 오픈 경로에서 별도 무음이 확인됐다.
- **v14 시작 프레임**: 먼저 영상을 숨긴 후 0초 탐색을 요청하고 seeking 종료 및 프레임 준비 후 표시한다. 회귀 검사 통과 및 사용자 수정 성공 확인.
- **v15 자동 오픈 오디오**: 드래그는 정상인데 자동 오픈만 BGM이 무음이라는 사용자 관찰에 따라 자동 오픈도 기존 sacred-open.wav 버퍼와 공유 AudioContext로 통일했다. 영상은 무음으로 두고 playing에서 현재 위치로 WAV를 재생하며 waiting/pause에서 중지한다. 버퍼가 없을 때만 영상 내장 오디오로 대체한다. Chrome 실제 자동 오픈에서 BGM 약 25.2% 유지와 복귀를 확인했다. **v15 iPad 최종 확인은 대기 중**이다. OS 내부 오디오 세션 원인까지 확정한 것은 아니다.

### 버전·커밋 이력

| 단계 | 커밋 | 변경 |
| --- | --- | --- |
| 기반 | 8666e80 | 쿠지 v31 복제 |
| v7 | ee4edbf | 소환서/소환 연출, 오디오, UI |
| v8 | 7ee9060 | BGM 한 곡 연속 루핑 |
| v9 | ec217dc | iOS 영상 배경 배치 수정 |
| v10 | c15eb48 | 준비 상태 및 입력 복구 |
| v11 | b49b159 | 지연 로딩, 첫 프레임, Blob 적용 시점 |
| 비교 진단 | 7e0c4f2 | v8/v9/v11 격리 비교 및 진단 |
| v12 | bb90e11 | iPad 호환 H.264 인코딩 |
| v13 | 2e04d5b | BGM GainNode 및 효과음 resume |
| v14 | 97727f3 | 자동 오픈 전 마지막 프레임 숨김 |
| v15 | 3afd03d | 자동 오픈 공유 Web Audio |
| v16 | ce1e68e | 자동 오픈 1.8배속 및 현장 흐름 정리 |
| v19 | 9bf42c8 | 영상 비율별 메인 배치 안정화 |
| v20 | 3db2500 | 드래그 안내를 소환서 내부로 이동 |
| v21 | 2ece506 | 드래그 진행률과 안내 UI 연결 |
| v24 | 2b345d2 | 쿨다운·무제한 반복 구간 배정 |
| v25 | 45e0757 | 대기 영상 중복 없는 무작위 순환 |
| v26 | 4fce9c1 | 골드 그라데이션 드래그 UI |
| v27 | ffff3b8 | 정지 손잡이와 순차 방향지시등 |
| v28 | 0b18ba2 | 당시 단일 메인 영상 디코더, PWA 자동 갱신, 공유 문서 정리 |
| 리소스 정리 | a5d8020 | 공유 폴더·최종/과거 자산 분류 |
| v29 | 62386b7 | 메인 영상 우선 로드, 다음 순환 영상 1개 선읽기 |
| v30 | e0215fc | iPad 첫 프레임 포스터(이후 v31에서 철회) |
| v31 | 1c57dac | 다음 영상 요소 사전 디코딩, 포스터 제거 |
| v32 | 379fb90 | 단일 미당첨 영상 재사용, 기존 기능 영향 점검, 사용자 확인 완료 |

각 수정의 근거와 당시 검증 범위, 이후 사용자 확인은 WORKLOG.md에 기록한다.

### 피규어 이벤트 운영 설정

개봉 영상은 전체 다운로드 후 Blob URL로 재생하여 Range 요청을 지원하지 않는 로컬 서버에서도 드래그 탐색이 가능하도록 한다. 자동 오픈은 정상 `play()` 재생을 사용하고 `ended` 이벤트에서 결과 화면으로 전환한다. 드래그로 끝까지 펼친 경우에는 화면에 이미 펼쳐진 마지막 프레임을 그대로 두고 즉시 결과 화면으로 전환하며, 마지막 프레임에서 `play()`를 호출하지 않는다. 끝 근처 탐색 위치를 `duration`으로 맞추는 브라우저에서 `play()`가 개봉 영상을 처음부터 다시 재생하는 문제를 막기 위해서다. 재생 실패 시 같은 당첨 결과로 재생을 재시도하며 재고를 추가 차감하지 않는다.

메인 영상은 관리자가 업로드한 대기 영상만 원본 비율로 중앙 표시한다(업로드가 없으면 영상 없이 기본 그라데이션). 영상 비율이 화면과 다르면 첫 디코딩 프레임에서 만든 최대 320px 정지 이미지를 확대·블러 처리해 여백을 채운다. 동일 영상을 배경에서 한 번 더 재생하지 않아 iPad의 디코딩·합성 부하를 줄인다. 다른 화면과 PWA 백그라운드에서는 영상을 정지하고 메인 복귀 시에만 재생한다. TOUCH 버튼은 텍스트 폭에 맞춰 중앙 정렬한다. 소환서 12개는 화면 높이에 맞춰 태블릿 4×3, 좁은 화면 3×4로 배치한다. 결과 카드는 화면 높이에 맞춰 확대했다.

소환서는 사운드 포함 `Sacred Scroll Reveal.mp4`(30fps)를 두 구간으로 나눈다. `03:10`은 3초 10프레임으로 해석하며, 해당 프레임까지는 `sacred-idle.mp4`를 반복 재생하고, 같은 구간의 사운드는 `sacred-idle.wav`를 Web Audio로 디코딩해 영상 위치에 맞춰 반복 재생한다(드래그 사운드와 같은 경로). Web Audio를 쓸 수 없으면 영상 자체의 오디오 트랙을 켠다. 관리자 음소거를 따른다. 터치하면 원본 `03:11`(프레임 인덱스 101, 약 3.367초)부터의 `sacred-open.mp4`를 표시하고, 드래그 진행률 0~100%를 개봉 영상 첫 프레임~마지막 프레임에 직접 매핑한다. 오른쪽으로 진행·왼쪽으로 역행하고, 중간에 놓거나 취소하면 재고 차감 없이 대기 루프로 돌아간다. 끝까지 진행하고 손을 놓으면 추첨을 한 번 확정한다. 원본은 보존하며 최적화본 생성은 `scripts/prepare-sacred-scroll.py`를 사용한다.

드래그 사운드는 `sacred-open.wav`를 Web Audio로 디코딩하여 해당 재생 위치의 최대 0.1초 구간을 짧게 겹쳐 재생한다. 왼쪽 드래그는 뒤집은 오디오 버퍼를 사용한다. 움직임이 멈추면 새 소리를 만들지 않으며 마지막 짧은 소리만 종료된다. 취소·화면 이탈·음소거 시 중지한다. 자동 오픈은 드래그용 짧은 소리를 중지하고 같은 sacred-open.wav 버퍼를 정상 속도로 재생한다. 영상 내장 오디오는 끄며, 버퍼가 준비되지 않았을 때만 내장 오디오를 대체 경로로 사용한다. 소환서 화면에 있는 동안(대기 루프 포함) BGM은 기존 볼륨의 25%로 낮추고 화면을 벗어나면 복귀하며 관리자 음소거를 따른다. Web Audio를 사용할 수 없는 환경에서도 자동 오픈의 영상 오디오는 사용할 수 있다.

영상 마지막 0.3초 동안 전체 화면을 흰색으로 덮는다. 드래그를 되돌리면 흰색도 줄어든다. 영상 종료 시 완전한 흰색 아래에서 결과 화면으로 전환하고 0.4초 동안 흰색을 걷어낸다. 이전 CSS 종이 효과는 사용하지 않는다.

피규어(제라투) 당첨이면 흰색 아래에서 결과 화면 대신 전체 화면 소환 연출을 먼저 보여준다. 작업 폴더의 `zeratu summon_34.mp4`(원본)를 `scripts/prepare-zeratu-summon.py`로 H.264 `zeratu-summon.mp4`와 포스터로 변환해 동봉한다. 원본(1080×1440)은 위·아래 112행이 검은 띠라 1080×1216으로 잘라낸다. 오버레이는 메인 대기 영상과 같은 방식으로 원본 비율 클립을 중앙에 두고 같은 영상을 확대·블러 처리해 여백을 채운다. 사운드는 같은 원본에서 뽑은 `zeratu-summon.wav`를 Web Audio로 디코딩해 영상이 실제로 재생되기 시작하는 순간(`playing`)부터 영상 위치에 맞춰 재생하고, 버퍼링으로 멈추면 함께 멈춘다(드래그·대기 사운드와 같은 경로). Web Audio를 쓸 수 없으면 영상 자체의 오디오 트랙을 켠다. 관리자 음소거를 따른다. 영상이 끝나면 0.4초 페이드로 걷히면서 기존 결과 화면(번개·색종이·팡파르)이 나온다. 재생 실패나 종료 이벤트 누락(영상 길이 + 3초) 시에도 결과 화면으로 넘어간다. 연출 중에는 처음으로 돌아가기와 유휴 복귀를 막는다. iPad에서 자동 오픈처럼 사용자 제스처 밖에서 시작되는 재생에도 소리가 나도록 소환서 화면에 들어갈 때 영상 요소를 한 번 미리 로드한다. 참가상은 흰색에서 바로 결과 화면으로 간다.

화면 구성: 메인 화면은 로고 락업만 두고 이벤트명 텍스트와 아레나 배경(이미지·기본 루프 영상)을 쓰지 않는다. 소환서 선택 화면의 부제는 없다. 소환서 열기 화면의 제목은 "소환서를 펼쳐주세요 · N번 소환서"를 같은 글꼴·크기로 한 줄에 표시하고, 소환서 영상은 소환서 선택 카드와 같은 보라 그라데이션·보라 테두리·글로우 박스 안에 정사각형으로 놓는다. 박스 내부 하단에는 약 75% 불투명도의 보라색 캡슐 손잡이(그립 점·이중 골드 화살표), 골드 레일, 촘촘한 방향 화살표 10개로 구성한 슬라이더 안내를 둔다. 대기 중에는 손잡이가 레일 왼쪽 출발점에 고정되고 골드 화살표만 자동차 순차 방향지시등처럼 좌→우로 점등한다. 드래그를 시작하면 자동 점등을 멈추고 손잡이와 누적 점등이 진행률을 따르며, 개봉 확정 시 안내를 숨긴다. 감속 모션 설정에서는 손잡이와 화살표를 정적으로 표시한다. 아래의 "스크롤을 오른쪽으로 밀거나 자동 오픈 버튼을 클릭하세요" 문구는 기존보다 50% 크게 표시한다. 결과 화면 배경은 다른 피규어 화면과 같은 아레나 이미지를 쓴다.

- 관리자 진입은 기존처럼 메인 좌상단을 3초 안에 5회 터치하고 PIN 입력(초기 0000).
- IP·상품에서 상품 종류를 **피규어 / 참가상**으로 지정하고 초기 수량과 실제 잔여 수량을 설정한다. 새 설치의 초기 재고는 0이다.
- 기본은 **재고 비례 추첨(OFF)**이다. 전체 IP의 노출 피규어·참가상을 합쳐 잔여 수량에 비례해 선택한다. 한 종류가 소진돼도 다른 종류로 계속 진행하며, 모든 노출 경품이 소진되면 종료한다.
- 설정의 **피규어 당첨 확률 사용**을 ON으로 바꾸면 별도 확률(0~100%)을 적용한다. **추첨 방식 저장**을 눌러 적용한다. 이때 피규어 당첨 여부를 먼저 결정하고, 해당 종류 안에서 잔여 수량에 비례해 선택한다. 초기 확률 표시값은 10%이며 OFF 동안은 적용하지 않는다.
- ON에서 피규어 소진 시 참가상만 지급하며, 참가상 소진 시 중지한다(피규어 확률 100% 제외). OFF로 전환하면 입력한 확률은 보존하고 재고 비례 방식으로 돌아간다. 전환은 재고·기존 로그를 변경하지 않는다. 스위치 필드가 없는 이전 설정도 OFF로 시작한다.
- 두 모드 모두 번들은 하위상품까지 재고 비례로 선택하고, 소환서 번호는 확률에 영향을 주지 않는다. 기존 쿨다운·행운상은 적용하지 않는다. 새 로그에는 적용한 추첨 방식과 ON일 때의 설정 확률을 함께 기록한다.
- 각 상품의 이미지와 함께 **상품 영상**(결과에서 무음 반복), **클릭 팝업 영상**을 설정할 수 있다. 팝업 동안 자동 복귀를 멈추고 닫은 뒤 다시 5초를 센다. 상품 영상은 60MB 이하.
- BGM은 기존 화면별 3슬롯(대기/선택/뽑기) 전환 외에 설정의 **재생 방식**에서 **한 곡 연속 루핑**을 고를 수 있다. 연속 모드에서는 **연속 재생 곡**으로 고른 슬롯 한 곡이 화면이 바뀌어도 끊기거나 다시 시작하지 않고 이어지며, 소환서 화면·개봉 연출 중 BGM 볼륨 자동 감소(25%)와 음소거는 그대로 적용된다. 고른 슬롯에 곡이 없으면 무음이다.
- 대기 영상, BGM, 효과음, 재고 편집, CSV, 백업·복원은 기존 기능을 재사용한다. NPC·라인업 등 이전 설정은 보존하지만 새 참가자 흐름에는 표시하지 않는다.
- 새 추첨은 재고와 로그를 `figure-draw.draw-state.v1`에 한 번에 저장한 후 연출한다. 저장 실패 시 추첨을 확정하지 않는다. 이전 개별 저장 키도 초기 로드에서 읽을 수 있다.

localStorage 키와 IndexedDB 이름, 서비스 워커 캐시 이름은 기존 앱과 분리했다. 새 서비스 워커는 자기 앱 접두사의 캐시만 정리한다. 기존 앱의 구형 서비스 워커는 같은 origin의 다른 캐시를 삭제할 수 있어, 두 앱을 같은 브라우저에서 함께 운영하기 전 기존 앱 쪽 정리 범위도 확인해야 한다.

## 구성

- `app/`: PWA 본체와 동봉 에셋
- `app/figure.js`, `app/figure.css`: 소환서 흐름 및 기존 백오피스 확장
- `app/draw-engine.js`: 종류별 확률·재고 추첨 규칙
- `resource/`: 공유용 이미지·영상·오디오·폰트 분류본(`final-assets` / `-old`)
- `index.html`: 앱으로 이동하는 루트 진입점
- `scripts/prepare-*.py`: 원본 영상에서 앱용 최적화본을 만드는 스크립트(Pillow, imageio-ffmpeg)
- `scripts/organize-share.ps1`: 작업 폴더 미디어를 공유 구조로 재분류하고 현재 앱 에셋 사본을 갱신하는 스크립트
- `scripts/create-share.ps1`: Git 이력과 재설치 가능한 로컬 도구를 제외한 공유용 ZIP 생성 스크립트
- `KUJI-REFERENCE-ANALYSIS.md`: 기존 코드 분석과 후속 보완 사항
- `WORKLOG.md`: 날짜별 작업 일지(무엇을 왜 바꿨는지)
- `.nojekyll`: GitHub Pages 정적 파일 배포

GitHub Pages는 main 브랜치 루트를 배포한다. 빌드 및 패키지 설치 과정은 없다.

로컬 확인: 프로젝트 폴더에서 `python -m http.server 8000` 실행 후 `http://localhost:8000/app/` 접속. 현재 베이스는 localhost에서 서비스 워커를 등록하지 않으므로 오프라인 설치 검증은 배포 주소에서 한다.

## 원본 에셋

공유용 미디어는 `resource/images`, `resource/videos`, `resource/audio`, `resource/fonts`로 종류별 분류한다. 각 유형의 `final-assets`에는 현재 `app/assets/`에 실제 적용된 파일의 공유용 사본을 둔다. 관리자 화면에서 업로드해 현장에서 사용하는 메인 루핑 영상과 상품 영상도 `resource/videos/final-assets/`에 포함한다. 원본·이전 시안·미사용 파일·미리보기 산출물은 `-old`에 보관한다. 기반 쿠지 참고 저장소는 `resource/project-reference/-old/`에 둔다. 세부 구조는 `resource/README.md`를 참고한다.

실제 PWA가 참조하는 정본은 계속 `app/assets/`다. `final-assets`는 전달 편의를 위한 사본이므로 앱 파일을 바꿀 때는 `app/assets/`와 앱/서비스워커 버전을 먼저 갱신한 뒤 `powershell -ExecutionPolicy Bypass -File scripts/organize-share.ps1`을 실행한다. 대용량 `resource/`와 `.kuji` 운영 백업은 Git에서 제외하지만 공유 ZIP에는 포함할 수 있다. 일반 Git 커밋에는 100MB를 넘는 원본을 넣지 않는다.

## 운영 전 검증

행사 수량·확률·참가상 콘텐츠 입력과 실기기 터치·오디오·오프라인·백업 복원 확인이 필요하다. 기존 앱 폴더의 README는 과거 쿠지 운영 가이드로, 새 동작은 이 문서와 실제 코드를 기준으로 확인한다. 기존 관리자 편집의 저장 실패 처리와 전체 백업 복원의 사전 검증은 추가 보완 대상이다.

검사: `node scripts/test-figure.cjs`. DOM 흐름 검사: 임시 `.tools/`에 jsdom 설치 후 `node scripts/test-figure-dom.cjs`. 에셋 준비 스크립트는 Pillow와 imageio-ffmpeg를 사용하며 원본을 덮어쓰지 않는다. 참조 영상 4~11초를 확인해 선택→개봉→금빛 연출→결과 순서를 반영했으며, 펼침은 실제 MP4 프레임 탐색을 드래그 진행률에 매핑하고 흰색 전환 효과를 함께 적용한다.

## 진단 도구와 회귀 검사

보고된 기기는 **iPad Pro 12.9형 3세대 / iPadOS 17.6.1**이다. 홈 화면 PWA와 Safari 탭 모두에서 소환서 재생 실패가 보고됐다. Chrome 정상 동작만으로 iPad 해결을 판정하지 않는다.

- app/review/: v8/v9/v11 격리 비교 화면. scripts/prepare-version-review.cjs로 생성하며 운영 설정·재고와 분리한다.
- app/media-diagnostics.html: 미디어 진단 화면. 사용자가 제공한 figure-v8-diagnostics.json, figure-v9-diagnostics.json, figure-v11-diagnostics.json은 원인 분석 자료다.

| 실행 명령 | 검증 범위 |
| --- | --- |
| node scripts/test-figure.cjs | 추첨 규칙, 구문, 에셋 |
| node scripts/test-figure-dom.cjs | 참가자 흐름, 재고 보존, 지연 탐색, 오디오 이벤트 |
| node scripts/test-scroll-audio.cjs | 반복/역방향/자동 오픈 버퍼, 오프셋, 대체 경로 |
| node scripts/test-audio-mix.cjs | 볼륨 속성 제약 모의 환경, Gain 감소/복귀, 효과음 resume |
| node scripts/test-figure-browser.cjs | 실제 Chrome MP4 지연 로딩·드래그·자동 오픈·오디오 RMS |
| node scripts/test-scroll-encoding.cjs | MP4 AVC/SPS 및 전체 디코딩 |
| node scripts/test-figure-sw.cjs | 서비스 워커 회귀 |
| node scripts/test-idle-video-cycle.cjs | 중복 없는 순환 및 초기 로드 구조 |
| node scripts/test-idle-video-browser.cjs | 실제 다음 영상 준비·반복 복귀·지연/취소/백그라운드 |
| node scripts/test-result-video-browser.cjs | 단일 미당첨 영상 반복·메인 복귀·재고·팝업·교체/삭제 |

DOM 검사는 로컬 .tools의 jsdom, 브라우저 검사는 Playwright Core와 시스템 Chrome, 인코딩 검사는 ffmpeg를 사용한다. 배포 앱 자체에는 이 도구들이 필요하지 않다. v15에서는 기본·DOM·소환서 오디오·오디오 믹스·실제 Chrome 검사 및 git diff --check를 통과했다. 인코딩·서비스 워커 검사는 v12에서 통과했으며 v15에서 재실행한 것으로 기록하지 않는다.

### iPad 확인 기준 — 사용자 확인 완료, 후속 변경 시 재사용

2026-09-18 사용자가 확인 완료를 보고했다. 아래 항목은 후속 배포·다른 기기 인수 시 재검사 기준으로 유지한다. 이번 메시지만으로 아래 모든 정량 조건의 개별 시험 로그가 확보됐다고 간주하지 않는다.

1. 메인 또는 관리자 설정 하단에서 `figure-v32` 확인. 운영 데이터 보존을 위해 PWA/웹사이트 데이터를 삭제하지 않는다.
2. 대기 영상을 여러 차례 순환해 **복귀 첫 화면부터 다음 영상이 시작**되는지 확인. 이전 영상·포스터·검은 프레임 노출, 복귀 조작 후 지연, 시작 2초 내 끊김 중 하나라도 있으면 미통과다. 즉시 돌아가기·결과 자동 복귀·반복 30회·백그라운드 복귀를 각각 확인한다. 최초 앱 실행은 사전 준비가 없는 별도 조건으로 측정한다.
3. 드래그 손잡이가 대기 중 왼쪽에 고정되고 골드 화살표만 순차 점등하는지, 실제 드래그에서만 손잡이가 움직이는지 확인.
4. 동일한 연속 BGM 설정으로 드래그/자동 오픈을 여러 번 비교하고, BGM 25% 감소·복귀와 소환서/당첨 연출 사운드를 확인.
5. 새 버전 배포 후 PWA 자동 새로고침 및 백그라운드 복귀를 확인한다. 재발 시 버전·진입 경로·음소거 설정·진단 결과를 기록한다.

v31 진단은 개발자 콘솔의 `window.idleVideoDiagnostics`에 최근 100건만 보관하며 서버로 전송하지 않는다. `enter.warm`은 복귀 시 다음 영상 준비 여부, `entry-frame.ms`는 진입 함수부터 다음 프레임 콜백까지의 시간, `waiting`/`failed`는 재생 대기·실패다. 프레임 콜백은 실제 화면 촬영과 동일한 증거가 아니므로 실기 관찰을 함께 확인한다. 강제로 파일 읽기를 지연한 테스트에서는 이전 영상 대신 선택된 다음 영상만 대상으로 삼지만, 준비가 끝나기 전 공백은 여전히 가능하다. 복귀 화면을 숨겨 대기를 다른 화면으로 옮기는 처리는 하지 않는다.

원본 검사: `node scripts/test-idle-video-browser.cjs "resource/videos/final-assets/main looping_2.mp4"`. 격리된 Chrome 프로필과 임시 로컬 서버를 사용하므로 운영 IndexedDB·재고·로그를 변경하지 않는다.

캐시 확인을 위해 운영 재고와 설정이 든 사이트 데이터를 무작정 삭제하지 않는다. 행사 전 오프라인 실행, 백업/복원, 운영 수량 검증은 별도로 남아 있다.

### v16 자동 오픈 배속 검토

자동 오픈에만 1.8배속을 적용했다. 분리된 Web Audio도 같은 재생률로 맞추며 버퍼링 후에는 영상 위치에서 다시 시작한다. 내장 오디오 대체 경로도 음높이 보정을 해제해 같은 배속 효과를 따른다. 종료는 기존 ended 이벤트로 처리하고 추첨·재고 확정 로직은 바꾸지 않았다. 마지막 0.3초 흰색 전환 구간은 실시간 약 0.167초로 짧아진다. 전체 행사 처리 시간 절감률은 선택·상품 수령·당첨 연출 시간에 따라 달라진다. iPad에서 프레임 끊김, 소리 동기화, BGM 25% 유지/복귀와 자동 오픈 후 드래그를 확인해야 한다.

 v16 검증 결과: test-figure / test-figure-dom / test-scroll-audio / test-audio-mix / test-figure-browser 및 git diff --check 통과. 실제 Chrome 자동 오픈 playbackRate=1.8, BGM RMS 0.137708 → 0.034414(약 25.0%) → 0.138314, 효과음 0.282204 확인. iPad 결과는 미확인.

## v16 현장 운영 화면 수정

- 메인 ZERATU 제거, LUCKY DRAW로 변경. 최대 글자 크기 90px → 135px(150%), 좁은 화면은 반응형 축소. 브랜드 묶음은 높이 1/3, TOUCH는 2/3 지점에 배치하고 타이틀 부유 애니메이션 제거.
- 관리자 > 설정 > 피규어 드로우에 소환서 선택 화면 숨김 추가. 기본 OFF. ON이면 무작위 소환서 번호로 개봉 화면 직행, 뒤로가기는 메인. 번호 선택만으로 추첨·재고 차감하지 않음. cfg.hideScrollSelection으로 저장하여 기존 설정 백업에 포함.
- 참가상 상단 메시지 변경, F상 배지 숨김, 노란 상품명 영역을 요청한 두 줄 비당첨 안내로 교체. 상품 이미지와 기존 재고·추첨 규칙은 유지.
- 자동 오픈 1.8배 및 공유 WAV 동기화 포함. 아직 로컬 작업이며 iPad 최종 검증과 푸시 전.

## v17 메인 화면 및 설정 가시성

참가상 상단은 “아쉽네요!”로 단축. LUCKY DRAW 아래 같은 폰트 EVENT 추가. 세로 화면의 중앙 16:9 영상 위 여백에 브랜드/타이틀, 아래 여백에 150% TOUCH 배치. 가로 화면은 상하 오버레이로 대응. 하단 좌측 @copyright Com2uS, 중앙 실제 APP_VER, 우측 Built by SSUPERWASABI 표시. 관리자 스크린샷에 존재하던 작은 숨김 체크박스를 별도 “현장 운영 · 시퀀스 간소화” 카드의 큰 스위치로 개선. 기본 OFF 및 즉시 저장 동작 유지. 앱/캐시 v17, 로컬 수정이며 푸시 전.

### v17 영상 비율별 자동 배치

실제 대기 영상 videoWidth/videoHeight와 contain 표시 크기로 배치 계산. 가로 영상은 여백이 충분하면 타이틀/TOUCH를 영상 위아래 여백에 배치하고, 세로·정사각형 영상 및 여백 부족 시 상하 오버레이 배치. 영상 변경 loadedmetadata/emptied, 영상 크기 변경, 창 크기 변경 및 메인 복귀 시 갱신. 폰트 로딩 완료 후 재계산. 영상 미등록 시 16:9 기준. 타이틀 하향 24px 조정 의도와 150% TOUCH, 하단 표기는 유지. 오디오/영상 재생 상태는 변경하지 않음.

## v18 배치 구분 및 버전 교정

이전 자동 배치는 가로·세로의 좌표 차이가 작았으며 버전을 v17로 남겨 테스트 구분이 어려웠다. 앱/캐시를 v18로 갱신. 가로는 실제 영상 위아래 여백, 세로는 타이틀 중심 화면 1/3+24px 및 TOUCH 중심 74%로 명확히 분리했다. 공간 부족 시 화면 안으로 제한한다.

## v19 세로 영상 중앙 묶음 배치

사용자 확인: 가로 영상은 2번 이미지의 기존 배치를 유지하고 세로 영상만 중앙에 모은다. 로고/타이틀 실제 높이 + 간격(화면 높이 4.5%, 28~72px) + TOUCH 높이를 합산해 전체 묶음을 수직 중앙에 배치한다. 가로 영상 위치 계산은 변경하지 않았다. 앱/캐시 v19, 커밋·푸시 전.

## 최신 게시 및 인수 상태

2026-09-18 기준 구현 정본은 v32(`379fb90`)이며 main 브랜치와 GitHub Pages에 반영됐다. 사용자는 배포 후 확인 완료를 보고했다. README·WORKLOG·영향 점검 보고서와 `writing reference`의 전체 작업 참고 자료/컨플루언스 원고를 인수 문서로 제공한다. 이전 버전별 기록의 “미커밋·미푸시·iPad 미검증”은 당시 상태이며 현재 배포 상태를 뜻하지 않는다. 새 기기의 운영 데이터 이관, 행사 수량, 오프라인·백업 복원 및 장시간 운영 조건은 해당 현장에 맞게 확인한다. 기존 `Figure-draw-app-v28-share.zip`은 이번 문서 작업에서 재생성하지 않았으므로 최신 v32 공유 패키지로 사용하지 않는다.

## 2026-09-14 — v20 소환서 내부 드래그 안내

외부 화살표를 드래그하는 현장 관찰에 따라 안내를 소환서 박스 내부 하단으로 이동. 화살표는 왼쪽 시작점에 고정하고 트레일로 방향 안내. pointer-events:none으로 박스가 터치를 받으며 드래그/개봉 중 안내를 숨긴다. 기존 드래그 및 자동 오픈 유지. 앱/캐시 v20, 커밋·푸시 전.

## 2026-09-14 — v21 화살표 드래그 진행률 연결

화살표와 트레일을 기존 --progress에 연결하여 오른쪽/왼쪽 드래그에 맞춰 이동한다. 드래그 중 안내를 숨기던 규칙을 제거하고 자동 개봉 확정 시 숨김은 유지한다. 취소·대기 루프 복귀 시 진행률을 0으로 초기화한다. 영상 탐색·추첨 규칙·오디오 경로는 유지. 앱/캐시 v21, 로컬 변경이며 커밋·푸시 전.

## v22 피규어 당첨 시간 제한

관리자 설정에 피규어 전체 쿨다운 ON/OFF 및 구간별 최대 1개 ON/OFF, 구간 분 단위 길이, 운영 시작 날짜/시각 추가. 기본 둘 다 OFF로 기존 설정 유지. 쿨다운 분은 기존 기본 설정에서 입력하고 시간 제한 저장으로 함께 저장한다. 구간은 시작 시각 기준으로 반복하며 종료 시각이나 하루 자동 재고 보충은 없다. 둘째 날은 운영 시작 날짜/시각과 당일 재고를 설정한다. 구간 미당첨 수량은 이월하지 않으며 정확한 소진 보장 없음. 두 제한 ON이면 모두 충족해야 피규어 당첨 가능. 제한 시 확률 100% 설정도 참가상으로 전환하고 참가상 없으면 차단. 추첨 확정 직전에 재검사하며 이미 확정한 결과의 영상 재시도는 재추첨하지 않는다. 기존 원자적 재고/로그 저장으로 새로고침 이후에도 로그에서 제한을 재계산한다. 로그 삭제는 제한 이력도 지우므로 운영 중 삭제하지 않아야 한다. 기기 로컬 시계/로그 기준으로 여러 기기 간 공유 제한은 지원하지 않는다. 상품별 기존 쿠지 쿨다운 대신 피규어 전체 공통 제한이다. 앱/캐시 v22, 커밋·푸시 전.

## v23 5구간 당첨 배정

구간 제한을 구간당 1개 배정 모드로 변경. 기본 길이 24분(수정 가능), 총 5구간 고정. 저장 시 구간별 무작위 시각을 생성해 설정/백업에 보존하고 동일 시작시각·길이 재저장이나 새로고침으로 다시 뽑지 않는다. 시각 이후 해당 구간 첫 참여자는 확률 설정 대신 피규어 당첨으로 처리하며 이 모드에서는 쿨다운을 무시한다. 로그에 해당 구간 피규어 당첨이 있으면 추가 당첨 차단. 시작 전·5구간 종료 후에는 참가상만 진행. 참여자/피규어 재고 없으면 지급 불가, 미지급분 이월 없음. 둘째 날 시작 날짜·시각과 재고 별도 설정. v22 복합 제한 설명은 이 정책으로 대체. 앱/캐시 v23, 로컬 변경.

## v24 구간 수 고정 제거

운영 시작 시각과 구간 길이만 설정하며 5구간 종료 조건 제거. 설정에 저장한 난수 시드와 구간 번호로 각 당첨 시각을 재현하여 새로고침·동일 설정 재저장 시 시각이 바뀌지 않음. 구간당 한 명 지급 후 다음 구간까지 제한, 참여자 없으면 미지급 및 이월 없음. 피규어 소진 후 참가상으로 진행하고 참가상도 없으면 중지. 당일 재고만 입력하며 별도 운영 종료 시각은 없음. v23의 고정 5구간 정책을 대체. 앱/캐시 v24, 미푸시.

## v25 대기 영상 중복 없는 무작위 순환

메인 진입 시 등록 영상 목록을 섞은 순서에서 하나씩 선택한다. 전체를 한 번씩 선택한 뒤 다시 섞으며 이전 묶음 마지막 영상과 다음 묶음 첫 영상의 연속 중복도 방지한다(영상 2개 이상). 현재 영상 자체의 반복 재생은 유지한다. 영상 추가/삭제 또는 앱 새로고침 시 순환을 새로 시작한다. 앱/캐시 v25, 커밋·푸시 전.

## v26 골드 그라데이션 드래그 화살표

소환서 박스 내부 하단의 원형 `drag-arrow.svg` 핸들과 트레일을 약 75% 불투명도의 보라색 캡슐 손잡이, 골드 레일, 방향 화살표 10개로 교체했다. 손잡이에는 그립 점과 이중 골드 화살표를 넣어 드래그 대상임을 명확히 했다. v27부터 대기 중 손잡이는 레일 좌측 출발점에 고정하고, 레일 화살표만 기존 시안보다 1.5배 굵은 12px 선과 85ms 간격으로 자동차 순차 방향지시등처럼 점등한다. 실제 드래그 중에는 자동 점등을 멈추고 손잡이와 누적 점등이 진행률을 따른다. 안내 문구는 기존 크기의 150%로 확대했다. 중간 취소 시 시작 상태로 복귀하고 개봉 확정 시 숨긴다. 첨부 이미지는 형태 참고용으로만 사용했으며 UI는 CSS로 직접 생성한다. 영상 탐색·오디오·추첨 규칙은 변경하지 않았다. 앱/캐시 v27, 원격 main 반영 완료.

## v28 메인 루핑 영상 디코딩 부하 개선

메인 대기 영상과 블러 배경에서 같은 영상을 두 번 재생하던 구조를 단일 영상 디코더로 변경했다. 블러 배경은 영상의 첫 디코딩 프레임을 최대 320px 저해상도 정지 이미지로 한 번만 생성해 사용한다. 다른 화면이나 PWA 백그라운드로 이동하면 대기 영상을 즉시 정지하고, 복귀 시에만 재생한다. 새 영상 Blob이 준비된 뒤 이전 Object URL을 해제해 로딩 중 배경 공백도 줄였다. 새 서비스워커가 활성화되면 화면을 자동 새로고침해 설치형 PWA 업데이트 지연도 줄인다. 앱/캐시 v28, 이번 배포에 포함.
